# 本地批量修复进度

## 比较符回退路径复验

- 扩展现有量价条件回归，覆盖中文比较词、ASCII `>`/`>=`、完整句/分段引用，以及开启/关闭模型语义审核。首次32例中8例失败：主完整性校验拒绝了符号比较冲突，但备用结构翻译仍遗漏符号，导致返回候选未被拒绝。
- 已补齐备用叶节点校验的同等比较符规则；与量价组合直接相关的52例通过，`git diff --check`通过。此为本地代码回归证据，尚不代表已更新运行服务或完成真实原句端到端验收。

目标以当前线程goal完整范围为准，未完成，不部署公网。

## 范围协商真实链路与剩余入口缺陷

- 上述自然语言入口缺陷已修复：内部CompileInput分离默认数据锚点与服务器日期校验上界，候选审核和编译使用同一上界，默认区间仍按数据水位。317项日期/候选回归通过，追加2项模型候选上界用例通过。已重启本地。
- 同一浏览器原句复验返回backtest_range_confirmation_required，显示原区间与建议区间；点击“接受建议范围”进入ready审阅页，量价AND、严格“超过”、MA10完整保留，开始回测仍需点击。截图`range-desktop.png`、`range-accepted.png`及对应text已保存并检查。初始截图截到了入场淡入状态，接受后截图为稳定状态；不作为新视觉设计验收。

- 已用300308 ready方案经结构化revision将结束日改为2026-09-16，返回backtest_range_confirmation_required；接受建议范围后ready，结束日2026-09-11，entry/exit完整相等，不自动run。
- 显式prepare通过后提交本地模拟回测，run:7221acd7928a412dacd0db48803930d2成功；summary/series/trades均返回，244曲线点、6笔成交（3买3卖）。末日净值与摘要finalEquityCny一致，runEvidence.strategyHash与接受后草稿一致。证据`outputs/batch-badcases-20260916/range-and-run-live.json`。运行标识为781054c+dirty，非clean-SHA公网验收。
- 浏览器自然语言直接指定同日期却失败：request_id=76624fd41ea44361a17f224b8897487f两次候选报backtest_end_after_as_of_date，最终candidate_provider_invalid_output。原因是前端as_of_date和后端effective_request均使用数据末日，把2026-09-16错误地当成未来时间，提前阻断协商。需要分离“默认区间锚点”和“用户显式日期允许上界”；当前仅结构化改参路径可用，不能宣称全部范围入口完成。

## 真实本地多轮与页面增量

- 300308原句真实请求返回201 ready、diagnostic_code为空；结构为收盘20日新高 AND 成交量严格gt_multiple(前5日均量1.5倍)，卖出first_of下的收盘价下穿MA10。数据准备不再被H股超额配售分类拒绝。验收脚本最初错误地直接读取exit.params，已按实际first_of结构校正并核验已保存响应，没有为测试路径错误重复发模型请求。证据`outputs/batch-badcases-20260916/breakout-live.json`；尚未据此宣称完整回测结果通过。

- 补充历史待确认草稿HTTP路由回归3例：分别回答“可以，按你说的来”“20”“是”，均返回ready revision2，完整策略与已展示候选一致，持久化状态run_requested与refresh_data为false。使用受控旧状态和真实FastAPI路由，不冒充真实模型旧会话验收。

- 已通过现有启动脚本更新本地8011/5184，health返回ok。此运行版本包含未提交改动，不能当作clean-SHA发布证据。
- 真实模型原句“平安银行，kdj超卖买入，然后10%止盈止损”返回201 ready，无诊断错误，并明确显示阈值20为系统建议、可修改；随后同草稿clarification-answers提交“改成30”返回ready revision2。断言股票、完整exit结构不变，entry.value为30，两轮run_requested均false。
- 独立真实浏览器重新输入同原句，检查桌面提示含“系统建议”，进入审阅区；桌面与390px手机截图已人工查看，展示J值低于20、止盈止损各10%、开始回测按钮，无需先确认阈值。未点击开始回测。
- 证据：`outputs/batch-badcases-20260916/multiturn-live.json`、`review-desktop.png`、`review-mobile.png`、`review-desktop.txt`。这是新建和修改路径证据，历史pending会话、区间建议浏览器接受以及完整回测结果仍待验收。

## 已有增量

- 自动补齐建议阈值并告知可修改；Catalog参数默认值与建议阈值分开。历史阈值确认状态支持接受已展示建议；KDJ/RSI/CCI及“按你说的来”“数值”“是”的编译回归覆盖。
- 候选、语义审核、候选编译和对话模块459项通过（随后还有报告文案和比较符校验增量，需各自验证）。真实隔离模型原句返回ready，尚需更新后的本地浏览器和旧会话实际验证。
- 报告预算不足通用原因改为“本次买入预算”，摘要改为“次买入”，不再把指标策略叫定投。

## 新定位

- 300308.SZ，2025-03-15至2026-09-11，直接调用当前EastmoneyCorporateActionReferenceAdapter.prepare复现：capital-structure ledger has an unclassified change reason: H股超额配售。接口返回了记录，不是已证明缺少日期覆盖。需核验股本变动是否影响A股权益，再处理分类；不能直接白名单放行所有未知变动。
- volume.relative执行与UI分别支持gt_multiple（严格大于）和gte_multiple（大于等于）；原有词法基线正则遗漏ASCII比较符，而且语义审核模式未执行该比较校验。已加入>、>=、≥等识别以及不可被模型审核绕过的严格比较校验，待针对回归及原句复验。

## 剩余

- 公司行动分类/错误分类修复、比较符原句复验。
- 实际接口与私有存储能力清单、有依据的覆盖区间交集与用户接受流程。
- 本地浏览器新建/旧会话多轮/修改参数验证，SPEC与Git收尾。

## 日期协商增量

- 已实现结束日期超出已导入水位时的建议流程：仅候选缩短区间通过完整preflight，才返回不可执行的backtest_range_confirmation_required预览，保留revision_base_strategy中的原区间；临时接口失败不生成建议。
- UI沿用原卡片样式，增加“建议回测范围”及建议日期，提供“接受建议范围”；接受后保存新范围并仍禁止自动启动回测。
- 本轮预检相关102项、前端契约101项通过，前端类型检查通过。新增接受流程与哈希/预览序列化正在直接验证。
- 尚未覆盖开始日早于个股可用范围、不同数据依赖交集、浏览器实际接受旅程，不能据此宣布整个范围协商需求完成。

### 上市前区间补充

- 已将接口核实的上市日期作为结构化内部边界，按请求历史起点与上市日差额保留本次预热需求后预填候选开始日，同时与最新导入日期取交集；候选仍须完整准备通过。
- 如果新范围校验失败，不能把原来上市前的非法策略保存成ready。合并回归首次发现3项该边界回归，已恢复原422门禁，只在新范围完整通过时提供确认预览。
- 完整组合回归560通过、3失败后，仅重跑发生修复的预检文件；其余通过项目无新改动不重复运行。浏览器及个股私有数据起点仍待验收。

## 公司行动增量验收

- 实际股本源记录：2026-08-31 H股超额配售，A股限售/流通增量均0，H股及总股本增量均8175000；2026-07-30首发H股上市，A股增量均0，H股及总股本增量均54500000。
- 新分类仅接受这两类发行原因且A股变动为0、其他类别无变动、H股正整数增量与总股本增量一致。字段缺失、混合A股变动或不平衡继续拒绝，不把H股拆并股或任意未知变动加入白名单。
- 未分类经济含义现在映射corporate_action_source_invalid，不再统一伪装成获取不可用；其他错误分类仍需继续检查。
- 62项公司行动与本地数据准备测试通过；真实300308.SZ同区间prepare返回3项公司行动，negativeSplitProof识别H股超额配售、首发H股上市、限制性股票和高管股份变动。此为数据源层验收，不是完整回测已通过。
