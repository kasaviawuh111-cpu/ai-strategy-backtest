# 公网005：前后台与多客户并发

2026年9月6日。用户明确要求将已完成的并发修改发布到现有公网，不增加账号、不另建站点。此次仅新增B34并发调度；004已有功能、真实模型思考返回、取数及Python计算口径保留。

## 发布包与本地验证

- 内容修订 `sha256:14e9669dea94945130937d16bb103a5a71333245c8f52e0a3d287d6e6cb281ac`，是源包清单摘要，不冒充Git提交SHA。
- 前台策略2路、后台报告1路，各另有8条等待；匿名浏览器各自限额，等待请求自动继续。计算队列仍为1个Python线程、2条等待；不是无限并发或多租户鉴权。[实现与本地证据](bugfix-concurrency-20260906.md)。
- 定向80项后端、28项前端接口、2项既有UI并发编辑测试、6项打包测试通过；Ruff、TypeScript及生产非Mock同源构建通过。
- 最终源包的真实公开工厂、独立临时数据库验证：自然语言美的RSI14上穿30且成交额5亿元买入，退出为持有满10交易日且RSI14高于55。草稿ready，`run:c024ca7b54484858b07e52308f5e0ea6`成功，242个净值点、6条成交活动；真实DeepSeek-v4-pro报告200，thinking=enabled。不是仅用health或页面200验收。
- 源码、前端资源逐文件核对清单；包内扫描无现有凭据值，未包含文档、用户数据库或本地密钥。原004留存用于回退。临时存储跨容器替换不保证记录保留，已告知用户，B29继续暂缓。

## 云端进度

CloudBase任务2090036已finished，版本ashare-strategy-preview-005承接100%流量，公开免登录不变。原地址不变；浏览器核对preview-meta及JS/CSS逐文件哈希与本包一致，跨来源POST仍403。运行源码已提交并推送私有仓库19220c1。

## 真实公网结果

- 客户A使用390×844移动尺寸Chromium实际UI，客户B使用另一独立浏览器环境的API与匿名标识；不是两台微信真机或高负载测试。
- 原贵州茅台回测`run:e5c985aa636d4f8e829905ca75c38b24`成功，后台报告开始分析后，A通过“换只股票试试”原位文本槽换东方财富；B同时从独立草稿换美的集团。两份新草稿均ready、ID独立；A买卖规则、日期、本金、执行约束逐字段保持。
- A新运行`run:d8ea5ddcc9014de68330a932750b9c93`、B新运行`run:0db336ce8d6c4735ad50406ea1496266`均succeeded，分别244个净值点、54／87条执行活动。两次新回测完成时，原报告仍未结束。活动数不当成成交笔数或完整回合数。
- 原报告约165秒后返回，runId与sourceResultHash仍对应原茅台运行。真实DeepSeek-v4-pro思考最大单事件15,801字符；实际移动页面DOM采样高度160px、scrollHeight593px、文本823字符，允许滚动。没有关闭思考返回。
- 浏览器采集器曾在回测前等待一条已取消请求的响应体，页面已ready；只放弃这条采集读取，没有重发策略或回测。所有场景断言和证据保存后，采集器在关闭时遇到该延迟响应报错。另起独立只读核验，重新GET同三笔运行、内容版本及结果哈希，全部通过；不把采集清理错误说成产品失败，也不隐去它。
- 完整机器摘要见[005验收](releases/public-005-acceptance.json)，源包见[005清单](releases/public-005-source-manifest.json)，原始响应保存在.artifacts/public-005-acceptance/。旧004基线保留。

## 留存范围

B35自然语言解析的两个新失败、B30微信回首页恢复、B02长历史、B06指标逐点、B07来源链、B09长对话、B12复合检索保留；B20低优先级，B22／B23／B29暂缓。不会将本次并发验证写成以上问题全部解决。
