# 下一批公网发布：已保存，暂不发布

更新：2026-09-06。用户已选手机方案 A，并明确公网与本地都开启模型返回文本展示。手机导航、反馈落点及统一滚动区已本地实现，保存到下一批。**尚未确认的设计不实施；本轮不推送、不部署。**

最新确认：用户通过报告页手机导航 v2，并明确同意把本批改动加入**下一轮公网更新**，不是立即发布。新增本地内容如下：

- B27追加：换股未匹配时明确“未找到对应的A股，请修改名称或代码”；查询中断独立说明，不能冒充名称无效。用户“股票换成ok”截图尚未核对该次服务异常的具体原因，不宣称已修复取数故障。

- 报告摘要移除“东方财富 Skill · 复权收益模拟”标签；保留日期、来源详情及底层证据，不改收益计算。

- B13追加：用户明确同意删除句尾重复的“（数据日期：……）”。调整行情问答的模型提示词：正文已说明日期时不再重复括注，保留唯一日期与来源信息；不使用前端正则拼接／裁切，不追溯改写历史消息。真实模型文案待验，下一批发布。

- B16追加：股票／买入／卖出槽位改为无独立框的文本编辑。进入槽位全选，保持焦点再次点击允许光标定位、拖选和局部修改；Tab进入同样全选。保留仅提交改动字段、全量修改、空字段禁止提交。2项组件交互测试、类型检查通过；实际浏览器点按和手机触摸待用户验收，未部署。

- B32：报告页返回／编辑独立一行，标题、股票信息、页签和正文在手机上左对齐，桌面保持不变。
- B28：策略设置内复用原位股票编辑与全 A 股联想、保存接口。
- B19最新：已按v4接入分组标题、逐条件编号虚线、无框区间入口；点击具体条件进入二级参数编辑。指标周期／阈值／目录内指标和触发方式、持有期／止损／回撤数值均写入实际策略；普通指标组支持且／或，旧费用日期保留。区间支持常用半年／一年／三年及原有自定义日期。标题按实际规则概括指标与特征，不复述长句。
- B19边界：当前DSL的退出顶层固定first_of，含持有期／止损／回撤的退出组不支持切成all；界面明确禁用并说明。事件、财务条件面板暂不编辑。10项定向前端测试、tsc、定向eslint通过；本地真实生成的美的案例已检查分组及编辑入口，未据此宣称修改后的真实回测、微信或公网验收通过。

  后续真实本地验证：在正式页面点击美的RSI示例→点买入RSI条件→阈值30改35→完成→开始回测，成功出现报告；2025-09-06至2026-09-06，策略2.05%、持有20.55%、最大回撤5.21%、3回合／6笔成交。审阅保留成交额5亿、卖出RSI55／止损5%／10交易日。此为一次参数修改后的真实页面闭环，不覆盖全部逻辑切换、所有指标、手机微信或公网；报告AI分析当时仍在进行。
- B33：高级研究设置默认展开；涨跌停处理三种模式补充与现有引擎一致的说明，不更改撮合计算。

本轮相关三组前端测试13项通过，TypeScript、定向 ESLint 与 diff 检查通过。已检查本地设置页面显示；不等同于新增设置换股真实回测、手机微信真机或公网验收。正式发布前仍按下方流程验证。

## 存档与线上基准

- 本地保存点：Git 标签 `codex/pending-public-20260906`。这是工作版本快照，不是发布通过标记；包含此前尚未提交但已经上线的源码，不能把全部 Git 差异当成新增热修复。
- 线上服务：`ashare-strategy-preview`，当前核对版本003，继续免登录公开访问。本轮不推送 Git、不部署、不改云配置。
- 003内容摘要：`sha256:244dd789e136c3eb8901c50b7ae525d2567cd17e5d3474ff0bab938e6284a8be`。
- 已部署包本地基准：`/private/tmp/ashare-public-mvp-release.7GFN7Y/source-manifest.json`。包内303个文件全部匹配清单；在保存点 `506815f` 时，打包白名单与之比较301个一致、2个生产文件变化。之后新增了本页记录的手机UI及公开入口修改；下次发布必须重新构建并生成新清单，不能沿用003产物。
- 003对照包已另存到项目 `.artifacts/release-baselines/public-003/`（约7MB，逐文件比对与临时目录一致），不再只依赖临时目录；内容清单同时纳入Git：`docs/releases/public-003-source-manifest.json`。归档不包含凭据或用户数据库，不改变线上版本。

## 本批已实现、尚未上线

| 项目 | 已保存内容 | 下批发布前要求 |
| --- | --- | --- |
| B07/B17 取数重试与恢复告知 | `ashare_lab/adapters/market_data/mx_saas.py`、`ashare_lab/application/skill_backtest_service.py`；原请求最多两次重试、退避、安全诊断、按运行隔离的真实重试／恢复进度 | 保留现有次数上限、取消语义、原股票／规则／日期；定向回归及本地真实链路后随批上线。历史证据见 `docs/bugfix-mx-retry-progress-20260906.md`，不把故障注入当成公网断网通过。 |
| B17 最新状态与模型返回文本 | 最新实际短语与已返回文本放进同一个160px滚动区；默认跟随最新内容，上翻阅读时暂停；公开入口 `include_model_reasoning=True` 与本地一致 | 仅展示上游实际返回内容，无文本时展示真实状态；公开异步入口透传／请求隔离用离线SSE验证，本地真实分析文本已实看，公网003尚未开启。 |
| B31 手机方案 A | 任务受理后回到对话看进度；保存或提交失败同样返回对话，显示原因和重试／审阅入口，保留参数 | 本地390px真实生成→受理→取数→报告已通过；手机微信真机及公网待用户验收。 |
| B32 手机返回导航 | 审阅顶部独立“返回对话”44px点击区，手机不再保留重复×；桌面保留原入口和分栏 | 只改变网页的手机审阅导航，不改变微信原生返回按钮；不实施B19条件分组。 |

配套测试、源码及记录一起存档。前端已有 `progressLabel` 展示能力，不能误报为003完全没有运行进度。

## 已保存设计／诊断，尚未实施

| 项目 | 保存位置及明确范围 | 当前门槛 |
| --- | --- | --- |
| B30 手机重新进入首页 chips 未成功 | `.design/mobile-recovery/`；已复现缺失 `AbortSignal.any` 时202受理后结果查询中断的兼容缺陷，但未证实是用户手机唯一根因 | 后续补兼容及原查询恢复，不能重新POST制造重复任务；需手机原场景验证。 |
| B19 执行边界剩余项 | `.design/strategy-condition-groups/`，保留各版及v4 | UI及常见参数本地已接入；含持仓状态退出条件的all组合需要先定义时点语义并扩展DSL／引擎，不能用界面开关伪装支持。事件／财务条件编辑另待补齐。 |
| B20 思考状态与耗时 | `.design/compact-processing/`及Bug清单 | 使用真实耗时；旧版已实现部分与新待确认部分分开验收。 |

`.design/stock-field-edit/`为已实施原位换股的设计历史，一起存档但不是本批新增；`.design/portfolio-highlight-review/`是独立持仓复盘设计，仅保留，不纳入本次回测发布范围。

## 保存范围与排除项

保存当前项目已跟踪修改、未跟踪源码／测试／依赖锁定／文档、公开A股名称代码目录，以及全部现有设计HTML和说明。设计原本被Git忽略，本次明确纳入快照。

密钥、环境凭据、用户会话数据库、行情缓存、依赖目录、构建产物、运行日志与私有临时证据不纳入Git快照，也不删除，继续留在本地。该快照是代码与设计存档，不是用户数据备份；长期存储仍为B29。

## 下一次发布顺序

1. 以003清单及本保存点核对未发布差异；A已选并本地实施，其他待确认设计仍需明确选择，不把“存起来”当成“所有待办都已做好”。
2. 实施已确认的手机范围，桌面行为保持；每项只跑影响范围测试和必要本地真实链路。
3. 用户本地体验确认后，再统一准备新内容清单与发布包；未完成项继续保留，不伪报通过。
4. 获得当次发布授权后，仅更新当前公开MVP；不覆盖旧站、不增加测试账号、不迁移或购买数据库。
5. 上线后核对手机首页chips、回测进度／恢复／失败可见性及真实报告。完整待办与逐项验收输入见 `docs/bug-backlog-and-acceptance.md`；B05保持已关闭。
