# 公网012：查询恢复、修改后回测接续与MACD

状态：云端任务2091553已finished，012承接100%流量；查询恢复与MACD的真实公网应用链路已通过。验收中仍有初始提交未取得响应、额外HTTP读回中断，单独保留，不宣称网络问题全部消失。[原公开免登录地址](https://ashare-strategy-preview-305722-11-1330091763.sh.run.tcloudbase.com/)

## 本批范围

- B30：同一已受理结果地址的GET有界自动重试、手动恢复、旧WebView兼容、取消隔离及准确的处理中／失败状态；没有重复提交模型请求。
- B30：没有旧回测报告时，明确要求按修改后条件回测也会接续执行；仅修改不自动执行。
- B06：复用已有Python引擎，以东方财富真实后复权日线计算MACD；不放松行情校验，不把供应商成品MACD字段标为已验证。
- 保留已上线功能、匿名访问、现有模型配置与真实过程文本展示。B35选方案后补股票等未完成项不冒充本批修复。

[查询恢复与本地验收](bugfix-preview-query-recovery-20260906.md) · [MACD与本地验收](bugfix-macd-derived-20260906.md)

## 冻结来源与发布前证据

运行源码提交 `28f617f`，已推送原私有仓库。使用上一轮成功构建的前端，运行源码内容与真实MACD本地验收指纹 `sha256:d927fbcaa266190fe50e412293300d1ce9d8894a40cf0ecabda26b505fb4ebbd` 相同，没有重复运行无改动的测试。

发布包304文件，相对011只修改两个后端应用文件、前端JS／CSS及index入口；旧哈希命名JS／CSS由新文件替代。领导汇报PDF及其未提交工作文件没有混入本批。包标识 `sha256:af40ca546bbf9b159b60d49b4912e1f90a9138d7df78c5d01b4ff818a490ff40`。[精确清单](releases/public-012-source-manifest.json)

本地关键路径有真实输入与最终结果：MACD直接生成报告；查询断连4次后恢复同一任务、修改后自动创建回测，首次行情连接超时，显式一次API重试后成功。已明确区分GET查询恢复与重新取行情，不宣称首次零故障。

## 发布过程

前两次代码包上传均HTTP200，但SDK调用UpdateCloudRunServer时分别遇到ECONNRESET／EPIPE。每次均检查最新任务和在线版本，仍为旧任务2090833及011全量，未观察到新发布任务，因此没有盲目重复已受理任务。

随后仅对本次发布调用使用独立HTTP连接，复用官方SDK的TC3签名及相同请求内容；先通过只读核对，再提交成功，返回任务2091553。没有改产品代码、系统代理、权限、资源规格或依赖。

环境配置除CODE_REVISION外原样保留：公开免登录；候选解析deepseek-v4-flash／thinking disabled，深度规划与报告deepseek-v4-pro／thinking enabled／high。保留既有快慢模型分工，不把配置开关当成本轮实际过程文本验收。

临时存储边界不变，更新可能丢失旧会话和报告；B29长期保存仍暂停。

## 公网验收

[紧凑验收证据](releases/public-012-acceptance.json)。以下均在原公网地址核对到本包的精确revision；仅在独立验收浏览器制造GET断连，不操作用户会话。

| 范围 | 实际观察 | 结论与边界 |
| --- | --- | --- |
| B30结果恢复及无旧报告的显式回测 | 首页茅台chip生成201 ready，未先回测；手动将均量周期20改15并明确重新回测。结果GET连续断连4次后，显示“等待恢复连接”和44px手机恢复按钮；收起进度后仍可点击。同地址恢复201 ready，`run_requested=true`，仅创建一次回测并succeeded。summary／series／trades均200，242曲线点、9活动、1完整交易。 | 该修复已发布且本次应用链路通过；未验证微信真机返回入口，也不等于初始POST丢失响应可自动恢复。 |
| B06 MACD派生 | 新会话手动输入“东方财富 macd 金叉买 死叉卖”，POST202→结果GET201 ready；MACD(12,26,9)金叉买／死叉卖。点击开始一次，任务succeeded，浏览器收到summary／series／trades均200，242曲线点、43活动、7完整交易；手机实际报告已查看。 | 真实后复权行情366行，非缓存命中；MACD来源为Python派生，供应商成品字段仍未宣称通过验证。后续额外独立summary GET发生socket hang up，原脚本FAILED状态保留，不抹掉失败。 |
| 实际过程文本 | B30验收期间进度接口实际返回非空reasoning，`reasoningObserved=true`；暂停与完成截图已查看。 | 不是只检查配置开关；本次未追加点击报告分析。产品自动发起的分析请求与重复回测分开计数。 |
| 用户再次反馈的美的完整原句 | 当前012新会话手动输入一次，201 ready，`diagnostic_code=null`；买入ALL保留RSI14上穿30及成交额>5亿元，卖出first_of保留RSI14>55、止损5%、持有10日。仅编译，未回测。 | 本次未复现，不能据此关闭用户报告的偶发解析失败；仍归B35 P0处理。 |

B30任务 `run:ae514367e6274601b399338827818abb`，服务返回结果哈希 `sha256:34b2c3eaed09807ab59a84d2b40d3fdf86f860b96108fd17fec0b7023ac726ed`；MACD任务 `run:ecfbaae74d9f443a8eb315e590c64271`，服务返回结果哈希 `sha256:730483270055276f5ac1014ef1a0e1637d47a6d22dbd5ea1b0511313639966f3`。这里记录的是服务返回哈希，不冒充离线重算整个结果包的哈希核验。

### 验收失败与剩余边界

- 初次B30脚本未处理CloudBase“确定访问(3s)”倒计时，0次业务POST即停止；保留证据后，只修验收脚本等待已观察到的按钮，下一次完整通过。不是产品编译失败。
- 首轮MACD在读取meta时TLS连接中断，0次POST。第二轮观察到一次初始策略POST，但未取得202／201结果；页面显示连接失败，最终脚本等待预览超时。没有结果地址，不能套用“已受理GET重连”成功结论；也不能仅凭浏览器证据断言服务端是否已执行。
- 独立MACD确认轮应用已经成功并在浏览器读回三类结果；随后额外`APIRequestContext`读取summary发生socket hang up。原始FAILED证据与成功浏览器响应同时保留，没有重提策略或回测掩盖该次错误。
- 公网连接错误仍出现“本地回测服务”措辞，属于剩余错误提示尾项；本批未修改该文案。B35选定方案后补股票、新的偶发解析反馈继续处理。
- 手机尺寸浏览器不是微信真机；临时存储不是长期保存；有限成功样本不是全部策略或高并发稳定性证明。

完整原始记录位于本机 `/private/tmp/ashare-public-012-validation.t9iHAO/`：`public-recovery`保留倒计时失败，`public-recovery-gate-retry`保留B30成功，`public-macd`／`public-macd-retry`保留两次前序失败，`public-macd-confirm`同时保留应用成功与额外读回失败，`public-midea-typed`保留美的单次编译观察。这些本机记录不是公网托管附件。

[当前剩余清单](bug-backlog-and-acceptance.md)
