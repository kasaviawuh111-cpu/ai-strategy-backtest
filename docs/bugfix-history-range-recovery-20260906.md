# 历史取数：上市边界、缺字段补查与恢复提示

2026-09-06。对应 B01／B02／B03／B07 的本次案例，局部完成，待用户验收及下批发布；不关闭这些大项。不推送、不部署。

## 行为

- 在股票身份与上市日期查验后、日行情取数前，拒绝包含上市前日期的请求。返回独立 `skill_history_before_listing`，带真实上市日和用户所选起点。
- 页面显示“请调整回测区间”，只提供“修改回测区间”，直接进入现有日期编辑页；不无效重试，不自动缩短区间，不改股票、买卖规则或成交设置。
- 若只是额外的指标预热请求早于上市日，而用户区间合法，仅将预热取数起点调整到上市日。实际回测日期保持不变，沿用已有指标缺值／未就绪不触发信号的规则。
- 缺字段／空字段组最多再查一次原股票、原区间、原字段组；沿用运行隔离的进度通道，显示“历史数据缺少必要字段，正在补查一次”。成功继续，失败明确缺失字段；不重取已经完成的其他组、不拼造数据。
- 日期轴不对齐、重复日期、分块重叠冲突等返回 `skill_history_dates_mismatch`；其他历史完整性错误返回 `skill_history_validation_failed`。日志记录安全错误码，不输出提供方正文／凭据。更细的其他校验分类仍可后续补齐。
- 网络故障继续沿用原有有限重试，上市日期不适用不进入网络重试。跨字段日期冲突不自动合并，保留人工重新读取入口。

## 验证

- 后端两个直接相关文件：46 passed；覆盖上市前无日行情调用、预热专用调整、原参数不变、缺字段一次恢复及两次都缺时停止、进度事件。
- 前端失败恢复定向3项通过；条件编辑／槽位回归7项通过。类型检查和定向 Ruff／ESLint 通过。
- 原失败任务 `run:78116b4886764c2c9ffdffc8f7817923` 的实际参数重新提交，新任务 `run:d73f5137e20c4a9bbbfd1a366d013baf` 返回 `skill_history_before_listing`：2021-04-09上市，所选区间从2020-09-06开始。约13秒完成身份／日期查验，没有执行交易或生成假报告。
- SQLite只读比对新旧任务：策略JSON完全相同；除本次显式强制读取标记外，成交设置JSON完全相同。
- 正式本地页面完整闭环：新会话输入下方验收句，任务 `run:f9d49147e1984aa281a048bf1364efd4` 显示准确上市日期提示；点击“修改回测区间”实际定位日期设置，手动选择近一年后再次开始，`run:a2f2dc29711941e79b142ad16206a374` 成功出报告。收益-8.44%、同股持有-23.53%、最大回撤20.40%、2个已完成回合、5笔成交；这说明流程成功，不代表策略盈利。后台比对只有起始日期由2020-09-06改为2025-09-06，其他策略字段及成交设置完全一致。报告AI分析仍在独立进行，不作为本次完成标准。
- 本次同时补齐此前条件编辑／简洁标题与测试中的数组空值保护，未改变B19策略语义及已验收槽位交互。

## 用户验收输入

输入“中金辐照300962.SZ，收盘价创前20日新高买入，持有满40个交易日卖出。回测2020年9月6日至2026年9月6日，本金100万元。”开始后应看到上市日期解释及修改区间入口。点击入口检查原规则与费用保留，手动改为近一年后再回测；不会自动把六年改成一年。

证据边界：缺字段恢复是故障注入测试，不能当作全部长历史字段均可取到；本项不证明北交所、全部事件财务数据、所有长历史、公网或手机真机均通过。
