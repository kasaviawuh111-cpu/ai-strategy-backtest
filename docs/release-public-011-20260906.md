# 公网011：推荐问解析修复（010同包重试）

2026-09-06。**已发布011、承接100%流量，两条真实公网首页推荐问均生成完整可编辑策略。** 首次010镜像推送失败后，使用同一精确包重试成功。继续使用[原公网地址](https://ashare-strategy-preview-305722-11-1330091763.sh.run.tcloudbase.com/)，公开免登录与临时存储不变。

## 范围与依据

只发布B35本次推荐问修复：可信股票上下文的同代码回显不再误判编造；逐条件引用提示与安全错误反馈补齐；日志关联请求ID及候选尝试。仍限制最多两次候选模型调用，名称解析、原文数值、方向及且／或门禁继续有效。[原因与验证](bugfix-parse-failure-diagnostics-20260906.md)

代码提交 `a61530f`；包标识 `sha256:35db0acae6534cbc23670cfb1c478658d478a278ac794a42a42362f00deb98d3`。304文件与009逐项对照，仅修改 `vibe_candidates.py`、`openai_compatible.py`、`api/middleware.py`，新增 `ports/request_context.py`，无删除。

前端、思考内容展示、行情缓存、Python回测引擎及撮合全部未改。模型传输文件只增加日志关联，不改思考返回；现有云配置除代码标识外原样提交，不新增账号、不关闭深度模型思考。PDF及其他未提交文件不在本次包中。

## 发布前验收

- 009原包生产装配真实点击首页茅台／美的均失败，分别复现可信代码误判与引用数量不匹配。修改后同入口4/4 ready，完整数值及逻辑一致。[对照证据](releases/local-chip-parse-repair-20260906.json)
- 冻结的010精确包再次实际点击两条推荐问，均202受理、201 + ready，耗时25.38／21.25秒；股票上下文分别为600519.SH、000333.SZ。茅台策略哈希 `bc16df5a134903491a792691e0a68670b3613760fd2b303074c511261b7d7945`，美的 `b6cb1a80bad637be9891adbebcdf0f0f275790b4eef0c69a0d146eb7986ac814`，与逐项核对版本一致。
- 新增20项定向测试通过，相关候选及传输回归通过；3项已证实的旧失败仍明确排除，未宣称全量测试全绿。ruff及差异检查通过。
- 以上到完整策略可编辑，未触发收益回测。前端与引擎未修改，沿用已有真实回测证据，不把本次解析成功写成新回测成功。

## 公网发布与验收

首次发布任务2090787，版本010失败，旧009仍100%流量，配置代码标识也已回到009。构建日志2602232387表明所有Docker构建步骤、项目安装、源码清单断言均通过；在推送镜像到TCR的最后阶段报 `blob unknown to registry - sha256:859d541ac57c978c40fda1e48b0617585117ef02219f8996a2d5ff9a5be0fa50`。这是镜像仓库上传阶段失败，不是应用启动或推荐问解析失败；不据此保证其根因一定只是瞬时网络问题。

随后对同一代码包发起一次重试，不改Dockerfile、依赖、模型或业务代码。任务2090833完成，版本011承接100%流量；镜像构建2602231912明确返回 `Image pushed successfully`。公网元信息的代码标识与上述精确包一致，JS/CSS静态资源哈希一致。

- 公网实际点击美的推荐问：202→201 + ready，35.36秒，请求关联 `ef04bf55-cbe9-4e51-8553-871544239ab8`。RSI14、30、5亿元、55、5%、10交易日及ALL／first_of全部保留。
- 公网实际点击茅台推荐问：202→201 + ready，57.40秒，请求关联 `6e98bb41-6f6e-4bb3-a573-a33a0f03adaa`。前20日收盘新高、前20日均量1.5倍、回撤8%、40交易日及ALL／first_of全部保留。
- 两次真实提交的策略哈希均与本地逐项核对版本一致，审阅页打开、编辑控件可用，移动尺寸截图已查看。均未提交回测，不冒充真实收益重跑或微信真机验收。
- 首次茅台验收浏览器在页面导航时出现 `ERR_TUNNEL_CONNECTION_FAILED`，尚未发送策略POST；新浏览器环境重试后成功。该失败原样保留，不能把包含这次导航的整个浏览器旅程宣称零失败。云端状态读取也有一次CloudBaseError及一次ECONNRESET，后续读取确认任务finished与011全量流量。
- 发布后再次核对：公开免登录；候选解析仍 `deepseek-v4-flash / thinking disabled`，深度规划／报告仍 `deepseek-v4-pro / thinking enabled / high`。现有快慢分工和思考展示未改，本轮没有重跑深度分析，既有实际文本证据继续保留。

[本地及公网验收摘要](releases/public-011-acceptance.json) · [精确源码清单](releases/public-011-source-manifest.json) · [最终部署状态](releases/public-011-deployment.json)

B35其他名称代码拼接与报告复核、B30微信真机返回首页等仍独立保留。[剩余清单](bug-backlog-and-acceptance.md)
