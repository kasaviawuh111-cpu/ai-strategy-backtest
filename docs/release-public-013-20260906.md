# 公网013：均线省略主语误拒修复

状态：已发布013、100%流量，任务2091956 finished。源码提交 `3d3448f`，公网元信息与冻结包一致；三条原话真实新会话均ready，茅台继续回测succeeded。[公网验收证据](releases/public-013-acceptance.json)／[云端状态](releases/public-013-deployment.json)。

## 确定原因与范围

用户原话“贵州茅台5日均线上穿20日均线时买入，下穿20日均线时卖出”明确给了两条均线，后半句省略重复的5日均线。012校验器逐句看原文，把卖出误认成股价穿越单均线：正确的MA5／MA20死叉被拒，错误的股价下穿MA20反而可通过。

公网012独立新会话真实复现：201 unsupported，`candidate_provider_invalid_output`；requestId `aece2705-6a05-43ec-912f-5f8039f26386`，进度ID `8d589db2-5c30-4576-8916-c904c726d8fe`。美的RSI同批一次公网ready，因此不能宣称服务整体宕机或每个输入必然失败，也不以一次成功否定偶发反馈。

[发布前三例公网矩阵](releases/public-012-before-013-parse-matrix.json)：茅台unsupported，美的RSI与平安价格ready。首次网络导航失败的尝试均0 POST，恢复后每条新会话只提交一次。MA业务终态后中止了一条进度GET，不是策略POST失败。

修改只涉及 `vibe_candidates.py` 的提示约束和原文验证视图：沿用唯一明确的前文均线主语；显式说“收盘价”则不继承。逐叶校验和条件覆盖使用同一解释；方向、周期、AND／OR及数值检查保留。候选DSL仍由模型生成，原文引用不补字、不改写。

## 本地验收

真实生产入口与真实模型、三条独立新会话单次手动输入均201 ready：上述茅台、美的RSI五条件、平安银行低于10元买／高于12元卖。已逐项核对股票、条件、参数、阈值和组合关系。不是只看HTTP200。

茅台继续回测 `run:5e7fe62e349e407fb8d52ae9a3b3ae81` succeeded，三类产物读回200，242曲线点、42交易活动；使用真实东方财富历史数据的现有磁盘缓存，未编造行情。模型报告的reasoning实际返回。首个测试脚本漏收数组形状的series／trades，成功后停止该脚本，另作同一run只读GET核对；未为掩盖脚本问题重新回测。

185项直接相关检查通过（新增9个正反例），Ruff及diff检查通过。排除的旧ALL退出测试在HEAD原代码同样失败，与此次无关；没有改无关测试预期。

[紧凑本地证据](releases/local-013-parse-20260906.json) · [精确发布清单](releases/public-013-source-manifest.json)

最终发布源码与真实本地验收版本仅多一个docstring中文逗号改英文逗号的lint修正；还原该字符后计算的运行源码哈希精确等于本地验收revision。没有未验证的执行逻辑变更。

## 发布约束

相对012仅一个后端源码文件变更，前端资产、所有已经上线能力、公开免登录、基模与思考配置保持原样。包revision `sha256:7d0a7b1da51aea86f4e5b857cc8fbeb2cc42fd1f82def540c55199afdc558353`。

## 公网验收结果

三个独立新会话各一次手动提交，全部201 ready，逐项检查股票、指标、参数、阈值与组合关系。茅台run `run:7fb7f3076d644e02aff42d10169db5d1` succeeded；汇总／曲线／交易均200，242点、42条交易活动、7个完整回合。真实数据366行，本次history为live。手机尺寸报告截图已查看，不冒充微信原机。

同一输入的本地缓存与公网live数据回测，策略收益、基准收益、最大回撤、回合数和胜率全部精确一致。公网013此轮浏览器在报告分析返回前关闭，未新增思考文本验收；前端字节与模型配置均未改动，沿用012已有实际证据，本地013也实际观察到reasoning。没有把配置相同说成新验收成功。

只关闭本次均线省略主语误判。B35其他多轮／方案绑定／报告分支、B30连接尾项等继续保留，不把三条抽测成功称为全量稳定率。美的完整原句本次ready不关闭用户既有偶发失败；同事／老板实际失败仍须按后台requestID、入口、具体拒绝码逐条归因，不能以验收会话替代真实后台审计。[剩余清单](bug-backlog-and-acceptance.md)

## 未包含在013中的下一补丁

以下为013冻结提交之后的本地工作区差异，尚未发布，不计入本次成功范围：

- `compile_strategy.py`：未绑定股票的方案模板先通过现有Catalog与交易口径检查；绑定／执行校验失败保留选定方案及股票上下文，返回执行校验诊断，并清除自动运行与刷新意图。
- `vibe_candidates.py`：两处叶节点／参数拒绝日志复用既有上下文日志入口，便于将失败关联到请求；不是扩大可执行条件范围。
- 对应测试也属于后续补丁；013的185项检查、三条公开编译及MA回测证据不能自动作为新补丁的完成证明。

这与013已发布的“省略均线主语”修复分开管理。真实后台审计及其余新反馈继续留在B35 P0，不在本文笼统宣称全部解决。
