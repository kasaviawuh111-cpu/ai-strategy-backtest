# 默认参数来源通用修复

目标：修复合法默认参数映射，同时不放行未知路径、显式值冲突或未经确认的规则。已更新本地5184，未发布公网；验收证据见文末。

## 已定位

- 实时复现“平安银行，kdj超卖买入，然后10%止盈止损”。模型生成KDJ J<20，并声明/entry/0/value为默认字段。Catalog允许period、k_smoothing、d_smoothing默认值，但没有该阈值默认值，故被defaulted_field_unconsumed拒绝。
- 旧Schema只约束defaulted_fields是字符串数组，缺少消费路径说明；自动重试也没有该错误的修复提示，两轮重复相同声明。
- 已添加通用Schema说明与静态修复反馈，说明有效指标参数路径、禁止阈值冒充Catalog默认值、不得删掉用户规则以消除错误。没有移除校验器。

## 验证与未解决问题

- 25项针对回归通过，含既有语义审核下的未知默认参数、参数/股票/来源损坏拒绝用例。
- 隔离临时服务实时复验不再出现该错误，模型仅声明3个有效参数默认路径；201 ready，但minute_execution_unavailable（诊断实例未配置分钟执行，不是5184回测验收）。
- 重要：该响应仍生成J<20且语义审核认可，尚不能证明未明确阈值被正确呈现为建议或澄清。这需要继续检查建议与确定条件边界，不能以消除错误码代替目标完成。
- 原始临时诊断日志/private/tmp/probe-default-provenance.log及-after.log仅捕获合成问句对应模型结构，不记录密钥。后续继续覆盖其他指标、组合与补充答案链路。

## 数值阈值确定性检查

- 转换候选时独立检查各指标阈值是否有对应引用中的数值依据，即使模型语义审核equivalent也不能消除该问题；完整候选进入既有semantic_confirmation_required流程，不删除止盈止损或股票。
- 保留明确语言本身定义的数值语义：放量高于均量的1倍、当日上涨/下跌相对昨收的0；不把它们当作任意建议阈值。金额阈值沿用已有单位换算。
- 隔离真实模型复验原句返回201 needs_clarification / semantic_confirmation_required，不再ready或生成失败。仍需验证澄清问题文本、补充答案后的完整规则、同类组合与5184更新。
- 全文件第一次回归258通过、2例当日涨跌误拦；补充明确零阈值语义后针对相关6项复验通过。不能把首次全文件运行表述为全绿。

## 默认标签规范化与补充对话

- 对确实存在且有值的指标/entry/N/value及/exit/N/value移除错误的Catalog默认标签，不修改值；确定性阈值来源检查仍保留确认门禁。未知参数、越界索引、非规范索引仍保留并被原校验拒绝。此逻辑覆盖指标种类和买卖两侧，不依赖模型重试修正标签。
- 实时补充对话：原句needs_clarification，回答“J值低于20买入，其他不变”后ready，股票000001.SZ、KDJ J<20、周期9/3/3、分钟止盈10%和止损10%均保留。诊断实例未启用分钟能力，仍是minute_execution_unavailable，不冒称本地完整回测通过。
- 尚需更新5184并用真实分钟配置完成端到端验收；不发布公网。

## 本地真实链路复验（2026-09-16）

- Keychain授权后后端恢复，8011 health正常、5184返回200。运行证据版本为ef5ae2630412607cf16acc89d3f482733ee2ba17+dirty，不是clean-SHA公网验收。
- 最新候选模块回归268项通过，独立计划与组合编译回归32项通过。
- 原句返回201 needs_clarification / semantic_confirmation_required；补充“J值低于20买入，其他不变”返回200 ready，无诊断错误。000001.SZ、KDJ J<20和周期9/3/3、分钟止盈止损各10%保留。
- prepare返回200 ready；提交202；run:c8c08b2068684c9a9ea4710a91902034最终succeeded。summary、series、trades均返回200，58321个净值点，2笔完整成交及1笔部分成交。未出现卖出成交，因此不能声称此样例验证了止盈止损成交分支。
- 草稿与报告区间一致：2025-09-11至2026-09-11。dataSnapshotId为price-plan-input:a306ddc830f5cdfc81094bdc59d85c7597fae0e5ffe2662ff2069439f8cd99a1；resultHash为sha256:ad4b9983d99c4734c0c91da4665ec05db7e3eb0b821a7bf3d624da1fc4bce3f4。
- 附带发现：报告将该指标策略的13次预算不足跳过称为“定投”，属于通用执行文案误用，不能把本次解析修复说成所有问题已清零。浏览器验收及该文案需后续核查。

## 完成审计

- 浏览器验收已补齐：真实5184页面输入原句，出现阈值确认与已识别规则；补充“J值低于20买入，其他不变”，点击“查看并修改”后，审阅页展示000001.SZ、KDJ J<20、分钟止盈/止损各10%，并可开始回测。截图及页面文本在outputs/default-provenance-acceptance-20260916/。这轮模型建议值为0而非上一轮20，仍进入确认，证明保护不依赖固定候选值。
- 原句真实回测证据见上一节；不为制造卖出成交而改变用户10%条件。另运行现有test_daily_facts_drive_actual_entry_then_minute_protection，1项通过，验证合成数据下日线买入与分钟保护实际平仓的连接；它不是该股票真实卖出成交证据。
- 默认字段路径、Catalog参数物化、阈值与参数默认值分离已由9ddf426、284d7f2、ef5ae26提交。相关代码相对HEAD无未提交差异，运行证据ef5ae26+dirty来自同一本地工作区；其他未提交修改未清除，未将其宣称为干净发布版。
- 必要回归：候选模块268项、独立计划及组合编译32项，加本次直接依赖1项，共301项。覆盖KDJ/RSI/PE、买卖两侧、无效路径、显式参数及来源损坏；真实原句补充对话和API完整回测、浏览器审阅均有证据。
- 本goal完成：本地版供用户验收，未发布公网。后续独立事项为预算不足报告文案，以及用户新提出的数据能力范围/日期调整确认交互；不把它们声称为本次已解决。
