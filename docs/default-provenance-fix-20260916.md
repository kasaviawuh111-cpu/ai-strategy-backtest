# 默认参数来源通用修复

目标：修复合法默认参数映射，同时不放行未知路径、显式值冲突或未经确认的规则。未完成，尚未更新5184或发布公网。

## 已定位

- 实时复现“平安银行，kdj超卖买入，然后10%止盈止损”。模型生成KDJ J<20，并声明/entry/0/value为默认字段。Catalog允许period、k_smoothing、d_smoothing默认值，但没有该阈值默认值，故被defaulted_field_unconsumed拒绝。
- 旧Schema只约束defaulted_fields是字符串数组，缺少消费路径说明；自动重试也没有该错误的修复提示，两轮重复相同声明。
- 已添加通用Schema说明与静态修复反馈，说明有效指标参数路径、禁止阈值冒充Catalog默认值、不得删掉用户规则以消除错误。没有移除校验器。

## 验证与未解决问题

- 25项针对回归通过，含既有语义审核下的未知默认参数、参数/股票/来源损坏拒绝用例。
- 隔离临时服务实时复验不再出现该错误，模型仅声明3个有效参数默认路径；201 ready，但minute_execution_unavailable（诊断实例未配置分钟执行，不是5184回测验收）。
- 重要：该响应仍生成J<20且语义审核认可，尚不能证明未明确阈值被正确呈现为建议或澄清。这需要继续检查建议与确定条件边界，不能以消除错误码代替目标完成。
- 原始临时诊断日志/private/tmp/probe-default-provenance.log及-after.log仅捕获合成问句对应模型结构，不记录密钥。后续继续覆盖其他指标、组合与补充答案链路。

## 数值阈值确定性检查

- 转换候选时独立检查各指标阈值是否有对应引用中的数值依据，即使模型语义审核equivalent也不能消除该问题；完整候选进入既有semantic_confirmation_required流程，不删除止盈止损或股票。
- 保留明确语言本身定义的数值语义：放量高于均量的1倍、当日上涨/下跌相对昨收的0；不把它们当作任意建议阈值。金额阈值沿用已有单位换算。
- 隔离真实模型复验原句返回201 needs_clarification / semantic_confirmation_required，不再ready或生成失败。仍需验证澄清问题文本、补充答案后的完整规则、同类组合与5184更新。
- 全文件第一次回归258通过、2例当日涨跌误拦；补充明确零阈值语义后针对相关6项复验通过。不能把首次全文件运行表述为全绿。

## 默认标签规范化与补充对话

- 对确实存在且有值的指标/entry/N/value及/exit/N/value移除错误的Catalog默认标签，不修改值；确定性阈值来源检查仍保留确认门禁。未知参数、越界索引、非规范索引仍保留并被原校验拒绝。此逻辑覆盖指标种类和买卖两侧，不依赖模型重试修正标签。
- 实时补充对话：原句needs_clarification，回答“J值低于20买入，其他不变”后ready，股票000001.SZ、KDJ J<20、周期9/3/3、分钟止盈10%和止损10%均保留。诊断实例未启用分钟能力，仍是minute_execution_unavailable，不冒称本地完整回测通过。
- 尚需更新5184并用真实分钟配置完成端到端验收；不发布公网。
