# 公网008：初始对话回复校验的有界修复

2026-09-06。**已发布008并承接100%流量，本次改动对应的本地真实输入与公网移动尺寸UI验收通过。** [打开公网](https://ashare-strategy-preview-305722-11-1330091763.sh.run.tcloudbase.com/)，仍为免登录、临时存储。

## 只修什么

007已支持“先承接、再补条件”，但实际公网输入“我是秦始皇”仍遇到模型回复校验拒绝。007容器在对应时间记录`unsafe_reply_or_multiple_questions`；没有原回复，不能断言究竟是问句数量还是禁用话术。此前程序在此处直接结束，用户只能看见模型无有效回复。

008只改一个运行文件`ashare_lab/adapters/language/vibe_clarification.py`：普通初始对话的结构或表达校验失败时，基于同一份原始输入让模型修正一次。与已有重试共享最多两次调用的预算；再次失败仍拒绝，不放宽事实、证券、执行授权或Catalog校验，不拼接固定回复，不让用户机械重述。

008和007的303文件清单逐项对照，只有上述文件不同。前端资产、Python回测引擎、并发调度、行情接线、模型配置与公开免登录入口均不改。007真实换股、饮食／天气续接、回测和报告分析的局部通过证据保留，不能把它说成007的人设场景也通过了，详见[007记录](release-public-007-20260906.md)。

## 当前可核对证据

- 代码提交`dd8d5ff`；`test_vibe_clarification.py`最近邻完整测试100项通过，包含结构错误、双问句、两次仍失败、总预算不增加等边界。
- 源包标识`sha256:35546c36a8341fc9eafe3c7476bde2a55159bbdbc5070fac6f42c7eafa0ea88f`。
- 精确包本地生产工厂以真实模型执行“我是秦始皇”通过，耗时199秒，3组具体股票／策略建议，未自动选定或回测。回复为“既然要集中趋势出击，我按强信号和流动性排了三组可编辑组合，分别观察放量金叉、突破短打和动量顺势；想先看哪一组？”。这是模型当次生成，不是固定模板；进度返回18,911字符模型文本。
- 初始对话修复后继续进入策略建议链路的2项集成fixture通过，测试提交`a672f93`。与上面的100项适配器测试分开计数，不冒充真实模型验收。
- CloudBase任务`2090285`完成，008状态normal、100%流量；创建版本296秒，发布4秒，无失败原因。公网元信息和2份前端资源SHA256与精确包一致。

## 公网用户体验

真实390×844浏览器首次输入“我是秦始皇”，策略请求`202 + Location`并使用原有匿名异步接线，最终`201`返回3组带买卖条件的方案。实际正文为：

> 按大趋势、集中建仓、长拿的方向，从成交活跃样本里挑了三组研究组合，可编辑后再看回测；想先看哪一组？

页面实际显示浪潮信息“放量突破集中进场”、亨通光电“趋势金叉果断跟随”、天孚通信“强势趋势持有纪律”；买卖行保留“模型建议”标识。不是让用户重新机械填写买卖参数，没有冒称已选择首项，没有产生回测POST。这些是当次模型和工具生成的待验证方案，不是固定样例或盈利证明。[实际页面截图](releases/public-008-persona-result.png)

过程文本真实返回9,784字符。已查看实际视口截图：短状态48.5px，文本变长后160px高、scrollHeight466px，框体完整处于390×844视口；全页scrollWidth390，无横向溢出。未把隐藏DOM说成可见，也未用状态短语伪造模型文本。本次完整方案约198秒才返回，这不是响应速度优化或所有输入都成功的承诺。

机器证据见[008验收摘要](releases/public-008-acceptance.json)，源文件见[008清单](releases/public-008-source-manifest.json)。007真实基线回测成功、同结果报告分析通过及21,482字符模型文本作为未改路径的证据保留；008没有为相同计算逻辑重复跑回测，不能把007运行编号写成008的新运行。

本次是移动尺寸Chromium，不替代B30的微信真机快捷入口验收。截图中候选未逐个选择或重跑，不对未执行路径作通过声明。

不承诺所有模型输出或供应商请求永不失败；本次修复的是已复现的“一次表达校验失败即结束”缺口。长对话及其他剩余事项见[当前待办](bug-backlog-and-acceptance.md)。
