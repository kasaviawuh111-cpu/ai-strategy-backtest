# 单只 A 股策略组合开发

目标：自然语言规则生成、审阅编辑、共享账户执行、报告恢复贯通；本地真实验收后再公开发布。不包含多股票组合回测。

## 本轮已核实

- 后端公共 StrategySpec 已有 independent_plans，保存买入与卖出两个独立计划；已有共享账户执行适配及 API 契约测试。早期文档中“只有内部合同”的描述不能代表当前代码。
- 前端 StrategySpec 尚未声明 independent_plans；模型语言适配目录中也尚未发现该字段。仍需核对编译入口、候选结构、编辑保存及报告恢复，不能宣布双计划端到端可用。
- 网格审阅卡此前会在任一网格侧存在时隐藏全部买卖行。现已在同一流程卡按买入、卖出、区间顺序保留网格侧和独立条件侧，并恢复独立条件编辑及区间编辑入口。具体网格参数仍放高级设置。
- 原组合 API 矩阵中两个失败断言把买卖成交误等同于完成清仓。改为从实际成交重算持仓、现金、费用、期末权益与清仓次数，不要求合法组合必须清仓。

## 验证范围

- 前端 SummaryCards.condition-order：12 项通过，TypeScript 检查通过。
- 后端 independent_price_plan_contract 与 composed_contract：23 项通过。包括三类计划买卖方向的九种配对，合成行情经过准备、执行、报告接口及账户核对；不属于真实行情或实时模型验收。
- 尚未完成本轮浏览器、真实模型、真实行情端到端验收。

## 下一批

1. 明确双计划在候选、编译与默认参数中的合法表示，保持共享账户字段一致与每侧独立规则。
2. 接通前端类型、摘要、参数编辑、保存与历史恢复；保证修改一侧不会丢掉另一侧。
3. 使用代表性单股跨族组合核对真实输入、草稿、执行终态、成交、净值与报告页面。
4. 完成本地交付后由用户验收；仍未完成的组合明确记录边界。

## 双计划前端传递增量

- 前端 StrategySpec 已声明 independent_plans；修改日期和初始资金后，保存修订及提交回测保留两侧原始参数、规则与组合执行策略。初始资金同步更新两侧合同字段，不累加成两份资金。
- 规则摘要分别读取 entry_plan 和 exit_plan，不再将合法双计划视为缺少买卖条件；确认及执行说明使用各腿独立时点，避免冒充统一日线信号。
- API contract 100 项与 TypeScript 检查通过，新增用例检查保存、提交、规则摘要和原对象不被修改。
- 两侧参数编辑器、实时模型生成及真实浏览器链路仍待接通；本批不宣称可完整验收。

## 双计划参数编辑增量

- 高级设置复用现有 PricePlanEditor，分为买入计划与卖出计划；共享账户字段仅在买入面板编辑，变更同步至两侧合同。
- 分腿计划固定其方向，网格只展示归属侧间距；普通条件编辑面板及重复通用费用面板不再用于双计划。
- 修改规则参数保留另一侧计划及 composed execution；提交回测的费用取计划参数，避免被旧草稿镜像覆盖。
- Params numeric 与 API contract 共 114 项通过，TypeScript 检查通过。新增交互回归核对卖出数量、共享滑点、保存及提交。
- 尚未完成浏览器可见页面核验；候选生成与编译的双计划接线仍是下一项必需工作。

## 双计划候选编译增量

- CandidateAst 可保存独立买卖计划，禁止同时夹带另一份单计划或信号规则，并检查明确的初始资金一致性。
- 编译为绑定策略或待选股模板时保留两侧原始参数、共享资金、日期与 composed 执行策略；能力列表包含两侧计划类型。
- 缺少股票时，两侧均有原文依据才保留完整选股提案；缺少任一侧依据不生成提案。
- 编译与独立计划 API 契约共 31 项通过，包括九类配对的参数保真、换股、资金冲突及原文依据缺失检查。
- 这仍是内部候选到策略的增量。模型响应 schema、完整 grounding 检查、真实输入与浏览器验收尚未完成，不代表自然语言入口已支持双计划。

## 模型候选合同增量

- BoundedCandidate 新增双计划和分侧原文引用；方向/共享账户由既有 IndependentPlanPair 合同校验，不允许夹带另一套规则。
- 原文引用转换、精确文本检查和计划预检覆盖两侧。转换至 AST 保留分侧证据，清除模型猜测的行情锚点，不清除明确固定锚点。
- 新计划默认值逐侧应用；定投默认金额有分侧来源路径。合法双计划不再被规则完整性助手误判为两侧缺失。
- 候选适配相关 265 项通过。新增九种配对检查两侧保留、缺失证据、伪造引用、规则冲突与资金冲突。
- 仍须接通生成提示、完整性分支、分钟数据语义、模型审核与编译 grounding 全链路，随后完成真实输入和页面验收；本增量尚不可作为端到端完成证据。

## 生成流程接线增量

- 生成提示描述双计划的分侧原文、共享账户和无隐含先后关系；显式顺序条件仍使用支持顺序的单计划。
- 结构完整候选过滤、待确认语义分支、分钟计划判断现在包含 independent_plans；两侧原文采用相同的精确引用规范化。
- 新增定期买入配到价卖出的完整候选转换回归，核对两侧参数与无错误缺规则诊断；九类配对核对分钟计划检测。
- 候选和编译相关共 288 项通过。这里的完整候选转换使用固定模型响应，不是实时模型生成证据。
- 后续仍须检查 candidate_semantic_review 中的单计划假设、实际编译与真实数据执行、编辑和报告页面；未上线。

## 语义复核增量

- 复核 executionContext 逐侧复用既有计划时钟，明确共享资金/持仓/T+1及无跨侧成交前置依赖，不再将双计划默认描述为日线信号。
- 复核提示要求检查双计划两侧，并区分独立执行和显式顺序；分侧来源仅作为已检查元数据，不伪装成额外交易条件。
- 完整候选指纹仍包含两侧来源，复核展示去除来源元数据不改变指纹绑定。
- 语义复核 104 项通过，包括九类配对时钟和真实复核请求构造的双侧内容/指纹检查。传输使用固定复核响应，仍不是实时模型证据。

## 完整编译入口检查

- 定期买入配到价卖出的固定模型响应通过候选转换后，调用 StrategyCompiler.compile 得到 READY，标的及两侧计划均保留。候选相关 10 项通过。
- 本地 8011/5184 均有监听进程；越过沙箱网络隔离检查 8011 /api/v1/health 返回 ok。健康状态不代表服务已加载本次提交，真实输入验收前仍须更新本地进程。

## 首条实时模型验证（未通过）

- 已用既有启动器重启本地：后端 PID 12492、前端 12493，健康返回 ok，加载至 ef9b846。
- 输入：东方财富，每月第一个交易日开盘买入100股，涨到30元卖出100股，独立执行；2025-01-02至2025-03-31，初始资金10万元。
- draft 04627a7e-5865-4339-a4d1-00a70490e0ca 返回 unsupported / candidate_provider_invalid_output。真实模型两次返回 HTTP 200，但结构校验均失败，不能算生成成功，未提交回测。
- 现有安全反馈未包含新双计划校验消息，仅留下 candidates/0:value_error。已将固定的双计划合同错误加入安全白名单及路径字段，便于模型修复和后续定位；不输出模型原文或凭据。
- 下一步加载诊断改动并重试，定位实际合同冲突，不能由此猜测失败根因。

## 实时模型重试已生成草稿

- 加载 7fafa6a 后重试同句：第一次诊断为顶层与计划初始资金不一致，模型收到安全反馈后第二次修复并生成 READY。
- draft 1e98aabe-25a1-4700-96e1-a30515fc4d77，hash sha256:111400b353afface8003027743ca8a8f915bca7e6541edc5880d4eca34a9c627。两侧分别 scheduled 买入100股、conditional 上涨至30元卖出100股；共享100000元，原日期保留，分侧原文证据保留。
- 未提交回测，不能声明真实执行通过。模型补充的仓位上限10000股、卖出重复1次等默认建议仍需在编辑器中可见核对。
- 返回 execution_assessment 错误落入日线信号说明，已新增双计划说明分支及回归；生成提示明确顶层和计划本金为同一账户金额，不进行静默金额覆盖。

## 首次真实数据执行（未通过）

- 保存模型生成策略到 combination-live-strategy-20260916.json，以 StrategySpec 规范化后重新计算哈希，与 READY 草稿 sha256:111400b353afface8003027743ca8a8f915bca7e6541edc5880d4eca34a9c627 完全一致，未修改日期、规则或本金。
- /backtest-runs/prepare 返回 200 ready:true，随后提交返回 202 queued，run:9dc367c2d48c4f40b26c363b391c194b。
- 最终 failed / minute_data_unavailable，日志底层原因 minute_data_outside_market_calendar。没有 summary/series/trades 成功结果，不能声明端到端通过。
- 准备检查仅通过不足以证明分钟数据对齐；下一步检查实际本地日历覆盖及分钟来源。严禁删除不对齐数据、缩短日期或改用日线来绕过此失败。

## 日历覆盖修复及执行结果

- 组合回放为首笔容量加载前一个交易日；旧日历从2025年开始，漏掉2024-12-31。通过既有 BaoStock 脚本新增 var/market-calendar-2024-2026.json（727个交易日），新旧2025年起的交易日完全相同，未覆盖旧文件。
- 当前本地启动通过 MINUTE_MARKET_CALENDAR_PATH 指向新文件。原策略哈希及回测日期不变，run:522f28cb30034b78a90aff290cf127a4 返回 succeeded，结果哈希 sha256:9a5ba1e7eeac73c5aad1923f8cc5eb539963dae4635126bb966dc6f8ca7f1bbe。
- summary/series/trades 均200；57个交易日，初始权益100000、期末99848.96。证据标记176229a+dirty，本地真实外置分钟数据，不是干净公网版本验收。
- 成交检查发现买入记录从2025-02-05开始，而输入要求每月第一个交易日且回测从2025-01-02开始。需继续核对1月首笔是否被排程边界遗漏；即使执行状态成功，业务验收仍未通过。

## 首期定投排程修复

- 根因：investment_schedule 从回测开始日枚举名义计划日，遗漏开始日前休市顺延到区间内的计划（1月1日顺延1月2日）。
- 现在从开始日所在月/周的起点枚举，以顺延后的实际交易日过滤区间；已在区间前执行的计划不补买。历史日历不足以证明顺延时不凭空增加前期计划。
- 月度/周度假期边界、无补买、日线定投、分钟定投、九类配对合计48项通过。原真实策略须更新本地后再次核对1月成交及期末账本，不能以单测代替。

## 首期修复后的真实复验

- 本地更新至6875749+dirty并保留扩展日历，原策略hash不变。run:c970cf894cff42719994d9667c6cf919 succeeded，结果hash sha256:cc5bbedf959689e527b13cb63dfa2b1a41bd4ac6fdab96ac90a7a918bb2a8b64。
- 实际买入：2025-01-02、02-05、03-03各100股，成交价25.60、23.32、23.25，费用5.03、5.02、5.02。首期遗漏已在真实回放中修复。
- 期末权益99541.93，最后净值99.54193，与100000初始权益换算一致。summary/series/trades均可读取，数据快照仍为price-plan-input:fd5cea30503d3d66dbc242e58685d4da507f443b70f150984672e57086f9aa68。
- 浏览器已确认本地首页可渲染。组合卡片、参数编辑及报告页面尚未完成渲染验收，其他代表组合仍需验证；不据此关闭目标或发布公网。

## 前端真实生成检查

- 修复 API 草稿标题没有使用双计划简短名称的问题，复用摘要标题函数；API contract 100项及TypeScript检查通过。
- 用Playwright实际输入同句，接口201 ready，审阅页显示买卖规则及高级设置。但本次生成页面仍出现长标题和定时规则的“满足条件时触发”文案，未达到UI验收要求。
- 尚需保留并检查此次生成响应的具体结构，区分双计划和其他合法组合表示，统一各结构的标题与触发说明；不能只凭双计划单测宣称页面修复。

## 桌面编辑与报告链路

- 已确认长标题是App外层重新拼接规则导致，b71dc36后真实页面标题为“定时买入、条件卖出”，买入显示“按日历安排·休市顺延”。
- Playwright真实模型生成后进入高级设置，两侧参数区域均可见。后续复用该响应仅避免重复生成；修订、预检、回测和报告请求均连接本地真实服务。
- 浏览器把买入100改为200股、卖出触发价30改为24元，保留卖出100股，修订201 ready且返回参数断言通过。随后进入真实报告，run:9c06ae749f114d6a8c8f4b6cc5a76de2。
- 实际卖出日志：1月2日触发，T+1锁定，日终撤单重建，1月3日09:31成交100股、23.90元。不存在1月2日卖出新买持仓；列表突出委托时间容易误读，仍须检查成交时间展示。
- 桌面截图在/private/tmp/combination-review.png、combination-params.png、combination-after-run.png，已查看报告截图。移动端与其他代表组合未完成，不作为全目标完成证明。

## 手机端与成交时间展示

- 报告委托行现在显式显示不同于委托时间的实际成交时间，避免T+1跨日记录被误解为当日卖出；不改变成交数据或引擎。
- result-chain 27项及TypeScript检查通过。390px手机浏览器完成审阅、高级参数、真实修订201 ready、回测及报告链路；候选沿用已保存真实响应。
- 页面无横向溢出，卖出行滚动后“成交2025-01-03 09:31”可见断言通过，截图/private/tmp/combination-mobile-trades.png。该场景桌面和手机均已验证，其他组合覆盖仍待完成。

### 手机端时间截断复核

- 截图复核发现原可见性断言不足以证明文字完整：窄列仍有省略号。现仅对委托/成交时间启用换行，未修改引擎或其他表格文字。
- 再看390px真实报告截图，1月2日委托与1月3日09:31成交均完整呈现，无横向溢出；TypeScript检查通过。此项只证明该场景移动端可读性，不扩大组合验收范围。

## 新增实时模型与真实行情组合复验

本批直接调用当前本地服务，不注入模型回复或行情。两例均201 ready → prepare 200 ready → 202 → succeeded，summary/series/trades均200。后端证据版本6875749+dirty，仍不是公网或clean-SHA验收。

### 定投与持有期限

- 输入：东方财富，每月第一个交易日开盘买100股，每轮首次实际成交后持有10个交易日卖出全部可卖持仓，清仓后继续按月买入；2025-01-02至03-31，本金10万元。
- 编译保留scheduled月初买入与holding_period=10、all_position退出，不额外强制先卖后买。
- run:5f2f5ab5788141c9935204414c9069a7；结果hash sha256:f27080650edfe0179b305ad6996fd82a8f424eb2752de249cd57aaf8c16f6fbc。
- 三轮买卖分别01-02/01-16、02-05/02-19、03-03/03-17，每次100股；与独立交易日日历逐对核对，间隔均10个交易日。逐笔现金增减重算为99847.32，期末持仓0、完成清仓3轮，与summary和最后净值一致。
- 完整响应暂存/private/tmp/combination-current-monthly_hold.json。尚未单独验收该例参数编辑页面。

### 指标且/或组合

- 输入：东方财富，日线MACD金叉且RSI低于50买入，MACD死叉或RSI高于70卖出；收盘确认次日开盘执行；相同区间与本金。
- 买入保留all，卖出first_of；阈值50/70及MACD默认参数保留，无隐含阶段顺序。
- run:9f2f6247c29a4032a3ad258ecd015d3a；结果hash sha256:573d57e7722d6243794de0512d7f1db21f9bd78334cc9de6b4a319dd526baf69。
- 买入信号all[true,true]于01-20收盘确认、01-21开盘代理成交；卖出any[true,false]于03-03收盘确认、03-04开盘代理成交。逐笔核对均为下一交易日；期末权益96207.78与series一致。
- 完整响应暂存/private/tmp/combination-current-and_or.json。此例验证解析、执行与报告，不代替所有指标组合、顺序计划或浏览器验收。

### 明确先买后卖

- 实时输入：东方财富先下跌到24元买100股，只有实际买入成交后才在相对该成交价上涨2%时卖100股，只执行一轮，不预设持仓；同一区间及10万元本金。
- 201 ready、prepare ready、202及succeeded；保留顺序conditional规则、repeat_cycles=1、初始/开仓持股0，没有转换为独立双计划。
- run:806f25a59ba742b398b6d7adfca8dced，结果hash sha256:ea2f2f939fdd6d6efb96a8b42bfc25acb5d067ac5068853a04786453e02b9325。
- 01-02 14:35买100股/23.94元，01-23 09:31触发卖出，09:32卖100股/24.18元。直接核对外置原始Parquet：09:31最高24.46，超过23.94×1.02=24.4188；下一分钟开盘24.20，扣滑点成交24.18。触发价不是保证成交价，未偷偷改为限价。
- 只有两笔成交，卖出触发晚于买入成交，现金与期末权益均100012.75。完整响应暂存/private/tmp/combination-current-sequence.json，尚未单独核验该例编辑页面。

### 本地启动日历保留

- 本地后端启动器在未指定MINUTE_MARKET_CALENDAR_PATH且扩展日历存在时选用已验证的2024–2026日历，保留显式覆盖；缺少扩展文件时沿用原路径。启动会说明选用来源，仍由回测检查覆盖与内容。
- 避免手动启动丢失上一轮环境覆盖而重新漏掉2024-12-31；zsh语法检查通过。当前服务继续使用同一扩展日历，不为该配置改动中断正在验收的服务。

## 定投退出规则编辑缺口修复

- 浏览器检查发现scheduled.exit_rules虽然执行正确，却未出现在高级设置；原编辑器只读取conditional.rules。这是本轮发现的实际产品缺口。
- 复用原条件编辑器呈现定投退出规则，修改写回exit_rules而非新建rules，保留月度买入参数。退出数量方式展示“全部持仓（保留底仓）”，不错误套用定投预算选项，也不允许把退出方向改成买入。
- 5项定投/条件编辑回归通过；旧精简测试fixture补齐真实草稿必需的execution费用字段。TypeScript检查通过。
- 浏览器复用上轮真实模型草稿，实际将持有10天改为12天，经本地revisions返回201 ready，断言exit_rules.sessions=12、all_position不变、月初买100股不变，未产生错误rules字段。此为真实编辑保存验证，不是重新模型生成。

## 指标组合与顺序计划编辑验收

- 浏览器载入本批真实模型草稿后操作高级设置，保存走真实revisions接口，未注入修订响应。
- 指标组合：买入RSI阈值50改45，201 ready；entry仍为all、exit仍为first_of，整个卖出树与原始草稿完全一致。
- 顺序计划：第二步止盈幅度2改3，201 ready；第一步买入规则完全一致、买卖顺序和repeat_cycles=1保留。
- 使用说明改为单只沪深A股支持买卖规则组合，保留数据核对门禁及不支持多股组合的边界；不宣称任意组合或公网完成。
