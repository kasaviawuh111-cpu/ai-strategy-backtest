# B14 / B15：真实会话验收记录

日期：2026-09-06。对象为本地正式页面 `http://127.0.0.1:5184/`、服务 `http://127.0.0.1:8011/`，使用独立测试标签页，不改用户原会话。未提交、推送或公开发布；工作区并非 clean-SHA。此前实施记录见 [optimization-dialogue](bugfix-optimization-dialogue-20260906.md)。

## B14 原句连续执行

基线输入：

> 东方财富，收盘价创前20日新高买入，收盘价下穿20日均线卖出。回测2025年9月5日至2026年9月5日，本金100万元，滑点5基点，佣金0，最低佣金0元。

真实模型生成草稿 `4b3c46ac-971d-496c-a9cc-f5eb1f8fa09a` revision 1。页面点击开始回测后得到 R0。基线分析 Prompt v11 的原文是“本区间策略实际亏损4.45%，未形成正收益。”以及“下一步按同股同区间回测候选，比较后再判断是否替换。”

1. 同一对话输入“你推荐的策略怎么还是亏钱的？”。真实回复为“该策略在回测区间亏损4.45%，确实未达盈利目标。下一步可检验缩短均线退出周期或加入放量确认，以观察是否能改善表现。”草稿仍为 revision 1，没有新增运行。页面实际显示该回复，没有自动回测。
2. 接着输入“改一下，给我一个你认为更合理的方案并回测”。草稿变为 revision 2，退出均线从20日改为10日；前端自动发起 R1，实际 succeeded，不停在候选或说明文字。其他 entry、instrument、execution、catalog、backtest 和全部 config 完全相等。
3. 页面截图显示新报告 -1.69%；R1 分析原文为“本次-1.69%，较上版-4.45%亏损收窄，交易4次。”仍未达到盈利目标。该结果不是对未来盈利能力的证明。

## B15 优化 chip 实际执行

R1 展示三条候选：5/20双均线金叉入场、10日持有期退出、5/20双均线死叉退出。点击第二条后得到 R2，实际执行策略与该批 `model-opt-2` 的完整 DSL 精确相等，而不是从标题重新解析。

候选来源为 R1 的 review `sha256:6b970efd5f89c14d99f54fb5253c7a833189075a10e956f4b5291389f61ace89`，候选 strategyHash 为 `sha256:e70c240e4f7c497d08b67ad792804807f67dcc2f8d207cb75cd65d65e0d36777`。只改变退出条件，entry、股票、日期、本金、执行约束和实际 config 全部保持。

R2 自动分析实际加载了 R0、R1、R2 和 R0/R1 两批已展示 review；服务日志 `backtest_review_history_loaded` 保留核验后的 run IDs、review 指纹和范围，缺失引用数为0。日志不记录用户原文、推理、密钥或行情正文，也不把工程信息加回页面。

R2 自动分析生成 RSI低于70过滤新高、12%止盈退出、5%止损退出，三条均不同于此前已经执行或展示的完整策略。

### 后半程隔离接口验证与新发现

测试标签页随后出现另一操作：把日期改为2025-01-05至2026-09-05并提交 `run:ff03b075560946aebf4eba3c14397008`，09:43:36终态为 `failed / skill_mx_transport_error`。这不属于上述三个测试运行。为避免覆盖该较新修改，停止操作这个标签页，保留原草稿 revision 4；后半程改用已有结构化恢复入口创建独立草稿，不能声称下列步骤完成了完整页面验收。

- 隔离草稿 `ea029e9a-3a3e-4e23-bf10-75f7598cfc99` 精确复制R2策略及配置，没有覆盖原会话。POST策略接口输入原句“这个已经试过了，还有别的方向吗？”，显式提供R0/R1/R2及三批已保存review。服务日志确认实际加载3个运行、3个review、缺失引用0；没有新增回测。
- 真实模型返回 `strategy_optimization_requested` 和新批次 `sha256:021b3ec12a6027dab611e1ea120ce01657093fd15f023f662b442851b3ec94c8`，Prompt v12。两条有效候选为“换手率高于2%确认新高”“连续3日上涨入场”。逐条以完整策略及有效条件身份核对，均不在此前3个运行、9条已展示候选中。
- 新的待确认草稿为 `86c50900-2d26-4740-9714-cc3af530c4fb` revision1。随后向它的澄清接口发送“采用第一个方案并回测”，同时提供包括R2旧、新两批在内的4个review引用，单个选择引用明确指向新批次。
- 返回 revision2、ready、run_requested=true，准确DSL为新批次的“新高且换手率高于2%”，不是旧批次的RSI条件。按前端同样的公开运行接口提交R3，实际succeeded，准确DSL与候选精确相等，费用及未改字段全保留。
- **历史失败：第一次模型的确认正文串到了旧批次。** 实际DSL正确，但正文误写“成交量达到过去20日均量1.5倍”。这是确认文字与执行规则不一致的真实失败，不能因运行正确而忽略。当时转入准确选中后的独立、隔离输入确认文案修复；后续v2最终复验见文末，原失败证据保留。

## 运行数据核对

全部表内运行均经过实际运行状态根接口、`/summary`、`/series`、`/trades` 读取，HTTP 200，终态 succeeded。历史行情来自先前真实东方财富日线的磁盘缓存，本次各自重新计算；没有把旧回测收益直接套用。R3换手率指标为本次live获取。费用均为滑点5基点、佣金0、最低佣金0，日期和100万元本金不变。

| 运行 | 实际退出规则 | 收益 | 最大回撤 | 完整回合 | 净值点／委托事件 |
| --- | --- | --- | --- | --- | --- |
| R0 `run:152dabcfba84433c976140d07bc202c3` | 下穿20日均线 | -4.453949% | -10.031826% | 3 | 243／18 |
| R1 `run:4c8aadc20a864590b4ee287d8bfb8bfe` | 下穿10日均线 | -1.691751% | -8.014995% | 4 | 243／24 |
| R2 `run:cbb792a2b09e4ef2b468710d34dc675b` | 实际成交后10个后续交易日退出 | +5.830234% | -8.404913% | 3 | 243／18 |
| R3 `run:45712a4540ed4bdfbe22e3474a9e5001` | 同R2，买入另加换手率高于2% | +5.830234% | -8.404913% | 3 | 243／18 |
| R4（v2复验）`run:f32910dfa6044e48b6b653c9891dc9db` | 同R3，准确确认后再次实际执行 | +5.830234% | -8.404913% | 3 | 243／18 |

结果指纹：

- R0：`sha256:99c8bdf0ede00703004fe32c83560de7fece6359d18a00958a9658e1f452b8fb`
- R1：`sha256:9441da68564b037fd9949f93a7a85faaa013b42720914c47621f73920eea3389`
- R2：`sha256:649a3a5b1dddbcd8bf998f8d67b6b01920fd3aaf871f94692c5ea4a2414716ff`
- R3：`sha256:016869bd4c22f555b3db3a15016b2ef9cffa4c539fc29c8f3b88cfbafad127bf`
- R4：`sha256:cdc19b8badf00c955ef2d0c60aea209961bf49974e352d9a1fe154a13baac827`

R3与R2的指标相同，不能称新增条件带来收益改善。这些结果说明产品链路可执行，不能证明样本外策略有效。该 Skill 路径没有完整 `runEvidence` 不可变数据／实现身份；相同股票、日期与缓存状态不等于已严格核验同一数据版本。

## 本轮追加修复及工程验证

- 内部排重只归一已由实际执行器证实无效的参数：`volume.relative` 非连续触发下的 `consecutive_days`。原DSL和公开strategyHash不改；有效周期、倍数及连续触发的天数仍可区分。不宣称已解决全部语义等价。
- 比较时核验已有 `runEvidence` 的不可变数据及实现身份，缺失或不同则限定比较，保留真实数值，不把结果差异全部归因于策略。主／修复Prompt均更新为自然说明，未给页面增加Provenance。
- 上述两点的两份直接相关后端文件共55 passed，Ruff通过，生产文件Pyright 0 errors。该次验证发生在确认文字串版修复之前。
- 前端此前“最新8条”陈旧断言已按实际前后端12条合同修复，未改产品。15条输入验证最新12条及顺序、60000字符边界；定向1 passed、完整 `client.test.ts` 20 passed。此前78过1败为历史检查记录，不冒充发生在本次补修之后。

用户体验验收仍保留，未公开发布。

## 追加：准确方案确认的隔离复验

- 隔离草稿 `57f6a5d4-be8d-4e9a-beef-c150ac19509e` 选择新批次第一条，请求 `e856e5b1f47444e395f92143ee026a38` 约10.9秒返回需澄清。实际结构校验错误在 `execution_settings / model_type`；日志没存原值，不能事后断言该次就是 null。旧草稿及配置保留，未提交运行。
- 补充明确 null 作为“无修改”空对象处理；false／0合法字段仍保留，数组／字符串／布尔对象仍拒绝。主编辑 Prompt v23 明确对象要求，日志只补类型不泄露值。直接文件56 passed，Ruff／Pyright通过；这是兼容修复，不是证明所有错误均消失。
- 再次隔离草稿 `410c5612-8cf1-4f58-a513-5ab777317507`，请求 `028756f01150432d81689d0bf71b63d2` 约14.9秒返回 ready、run_requested=true，准确选中“新高且换手率高于2%”，全部配置与R2相等。本次未实际提交回测。
- 该次回复不再串成旧量能方案，但仍多说了退出晚一个开盘日，以及“本轮不重新获取数据”。前者不符合实际持有期执行；后者不能从“不强制刷新”推断，因为未命中缓存仍会取数。因此仅算局部修复，不登记整条通过。
- 独立确认 Prompt v2 改为1—2句、只确认股票／关键条件／执行意图，不擅自扩写买卖时点或缓存行为；持有N个交易日不再加一次“下一交易日”。6个定向检查通过，Ruff／Pyright通过。该阶段尚未真实模型复验，因此当时保持B15未关闭；这是历史阶段说明，最终复验结果见下文。没有改DSL或放宽执行校验。

## B15 v2 最终本地隔离复验：确认、实际运行及真实分析

本节依据 `var/b15-confirmation-v2-live-20260906.json`、`var/ashare.db` 中指定草稿／运行／review，以及本地服务请求日志只读核验。补齐前述v2未复验阶段，保留先前确认串版、结构校验和文案越界的失败记录。本节是隔离草稿的公开API链路验收，不是完整页面交互验收。

### 真模型确认

- 隔离草稿 `cbad9ead-688f-47ae-b593-01befb768960` 从准确R2策略恢复，输入仍为“采用第一个方案并回测”，明确选择新批次 `sha256:021b3ec12a6027dab611e1ea120ce01657093fd15f023f662b442851b3ec94c8` 的第一条候选。
- 请求 `371ae74f9d5e4421b38531808d7cfe7e`，客户端记录17.904秒；`POST /api/v1/strategy-drafts/cbad9ead-688f-47ae-b593-01befb768960/revisions/1/clarification-answers` 返回HTTP 200、accepted。数据库保存revision2、ready、run_requested=true、is_strategy_edit=true、refresh_data=false。
- 实际确认正文从该草稿revision2对应的 `dialogue_drafts.turns_json` 读取，与保存JSON一致。采集脚本最初读取了错误响应字段，随后用同一草稿的持久正文补正，没有重新调用模型或拼写回复。

> 已选定东方财富（300059.SZ）策略：收盘价创20日新高且换手率高于2%时买入，持有10个交易日退出；本轮将按此执行回测。

正文准确对应选中方案，没有再混入旧量能条件、额外延后一个开盘日或承诺“不重新获取数据”。数据库确认策略、保存的expected_strategy及后续运行strategy_json逐字段相等，策略hash均为 `sha256:f1fafb531f0fba58b61748f73084ccfd1ec72db8be8d8c7346947a30cfe7c705`。股票、退出规则、执行约束、Catalog、日期、本金及全部配置与基准保持一致；只叠加已选定的换手率确认。

### 实际执行R4

- 通过公开运行接口实际提交 `run:f32910dfa6044e48b6b653c9891dc9db`，数据库创建时间为2026-09-06 10:34:20.391664 UTC，终态为succeeded。根状态、summary、series、trades的已保存请求日志均为HTTP 200。
- 收益为 **+5.8302337%**，最大回撤 **-8.404913%**，3个完整回合，243个净值点、18条执行活动。与R2、R3的原始数值相同，不能称新增条件或文案修复带来收益改善。
- 本次历史行情和换手率指标均从此前真实数据的磁盘缓存读取，实际重新计算；不是直接套用旧运行结果。`refresh_data=false` 只表示未强制刷新，不能扩写为任何情况下都不取数。
- 只读重算结果指纹与保存的 `sha256:cdc19b8badf00c955ef2d0c60aea209961bf49974e352d9a1fe154a13baac827` 一致。该核验仅证明结果包内部指纹一致，不补足缺失的不可变数据／实现版本身份。

### 真实分析与候选排重

- 分析请求 `db57390c876c4717bad3c1c0d8cc2eb0` 约124.75秒返回HTTP 200。保存的模型来源为 `deepseek / deepseek-v4-pro`，`backtest-review.prompt.v12 / backtest-review.v2`；review指纹为 `sha256:625d4696eaa09061ec4e98166f71a7e984271c7f799c8adbbe5a225e162c28a3`，sourceResultHash精确指向R4结果。
- 日志确认分析加载R0、R1、R2、R3、R4共5个已完成运行和4批已保存review，缺失引用0。实际analysis原文为“新旧两版真实收益同为+5.83%、回撤同为-8.40%，数据版本尚未核齐，差异待确认。”没有把同股票、同区间、同缓存状态当成同一数据版本证明。
- 新候选为“RSI低于50退出”“15%止盈/4%止损退出”“振幅高于5%确认新高”。按当前有效条件身份规则只读重算，历史已执行／已展示策略共12种有效身份；本批3条互不重复，且全部不同于这12种。该检查仍只归一已证实无效的参数，不宣称覆盖所有数学等价策略。
- review的evidenceGrade为insufficient，原因是“完整交易仅 3 次，无法稳定评价胜率或夏普”。三条新候选尚未执行，不称它们已改善收益。

本轮通过准确方案的真模型确认→实际运行→真实分析这段本地隔离API链路复验。完整页面交互与用户体验验收仍保留，B15不据此登记为全部关闭；未公开发布，也不构成clean-SHA或样本外策略有效性证明。
