# 本地验收与发布准备

2026-09-05 · 当前未提交工作区 · 尚未公开部署

## 可以本地验收

- 页面：http://127.0.0.1:5184/；后端8011。现有启动入口 `scripts/start_local_fullstack_review.command`，不使用默认5173开发入口。
- 处理过程默认展开；手动收起后不被流更新打断；完成收起，新一轮重新展开。结果和推荐问题先出，简短分析随后加载。
- 原始100条已逐项通过真实模型/真实数据验收，包含连续20轮、条件修改、换股、来源追问、原方案重跑、缓存及强制刷新。单元测试和构建不计入这100条。
- 最后额外成交量原句及真实优化chip也已完成页面闭环。提高阈值后的零成交如实展示，不能把“成功执行”解释为“策略更优”。

## 运行与证据边界

- 使用DeepSeek，联网通过独立检索后交给模型分析；行情来自东方财富MX。当前链路没有BaoStock后备，仓库仍保留旧独立适配器。
- 历史日线可live获取、内存复用、forced重新取数；五只白名单股票可落盘，其余也可能命中进程内缓存。均线、相对量等部分指标由Skill日线按现有公式计算。
- 目前是日线信号、下一可交易日开盘价代理，不是分钟撮合或实际证券账户。资金收益按提供方复权序列和配置费用模拟。
- 模型分析仍可能较慢；最后基线分析约82秒。候选偶尔重复先前规则，来源无发布日期时不保证近期性。100条通过不代表任意输入无错或策略盈利。

## 发布前剩余门槛

1. 用户在本地确认当前交互和效果后，再执行公开发布。
2. 发布配置必须显式关闭Mock、确认同源API及服务端凭据；不能直接照搬默认开发或固定历史日期的旧构建命令。
3. 如打包当前工作区，使用明确代码清单；不打包`.env`、数据库、原始响应、缓存、虚拟环境或会话历史。未执行大范围删除或公开上传数据。
4. 当前Live构建已通过，临时产物 `/tmp/ashare-review-build.gvEB4n` 只作本地检查；未作为已发布版本。

## 证据

- 原始输入：`docs/local-acceptance-100-cases.json`
- 逐项语义复核及所有中途失败、修复记录：`var/acceptance-100/semantic-review.md`
- 真实页面、槽位、优化按钮及默认展开：`var/acceptance-100/ui-d20-manual.md`
- 3/10均线真实取数与原失败策略重跑：`var/acceptance-100/ma3-10-diagnostic.md`

只记录公开输出及执行结果，不保存模型私有推理正文或API Key。
