# 公网006：多轮承接与失败原因修复

2026-09-06。用户已授权修好后发布到原CloudBase公网。**006已切流，但公网发布验收未通过；由[007修正版](release-public-007-20260906.md)接替，不能标为最终完成。** 不另建地址、不新增账号。

## 范围

先承接最新表达，再主动补成可编辑策略；普通缺省参数不默认反复追问。修复闲聊被错误当作策略编辑、旧失败污染续接、首字母换股识别、候选正文丢失风格承接，以及宽泛的数据错误说明。详见[实现、验收场景与边界](bugfix-conversation-recovery-20260906.md)。

## 发布前验证

- 最后审核的多轮／编辑／建议定向集138项通过、1项既有失败；后者用HEAD原代码内存复现，是长篇关税观点案例的候选无效输出，不放宽门禁掩盖。最终承接上下文另10项通过；名录与错误原因、前端失败卡的定向验证通过，Pyright、Ruff及非Mock同源生产构建通过。并非全仓所有测试全绿。
- 真实模型和东方财富数据在独立临时生产工厂运行。最终精确包草稿ready，`run:c6e24b008c1743b3a17c98ed9d1c28ad`成功，243个净值点、54条成交／执行活动；真实DeepSeek-v4-pro报告200，绑定相同结果哈希，思考最大单事件15,672字符。活动数不冒充成交笔数或盈利回合数。
- 同轮前一个候选包完整经过dfcf换股→安全支持→饮食→天气→恢复均线规则→低估值建议，原策略／设置未被闲聊改掉；随后发现并修复人设介绍被覆盖的问题，只在本地中间包验证，不将中间包上线。
- 最终本地390×844真实UI输入“我是秦始皇”，204.3秒后模型承接“帝王气场”，展示三组含买卖条件的可编辑股票方案；没有声称已选第一组、没有回测POST、无横向溢出。是移动尺寸Chromium，不是微信真机；没有将这一次选择前展示扩写为候选全部执行验证。

## 包与发布操作

- 源码提交`49ed17b`已推送私有仓库；[006源包清单](releases/public-006-source-manifest.json)包含303个文件。内容修订`sha256:98a71ddf6fd24a17ff4c5705159005edd3b4ca344195cc3ceb8b47e08ae0f3b9`是包内容摘要，不是Git SHA。
- 已逐文件核对源包和当前工作树，包内未包含本地数据库、凭据或用户文档。保留旧005源包与版本用于对照。
- CloudBase任务2090210；原CPU、内存、实例数量、匿名访问与模型配置均保留，仅更新代码内容版本。没有新增数据库或提高付费规格。

## 公网发现的发布配置遗漏

- 任务2090210完成，006曾承接100%流量。公网草稿ready，真实`run:8431adbc0a804d7886545c712040794d`成功，结果哈希`sha256:5038b1cb870d92fb3ed6cd20f2b56705e7f8b6aaa3681a0cb9d4ad9f1642a4c5`。
- 验收器随后没有收到报告POST的异步Location。排查编译后的JS确认：构建时漏传`VITE_PRIVATE_PREVIEW=true`，005中存在的`respond-async`和`backtest-preview-client`均被006构建移除。不是回测Python计算失败，也不是服务端主动关闭thinking；但它丢失了原有异步请求与匿名队列标识，长请求承受连接限制，因此不能通过发布验收。
- 之前TestClient验收主动加了Prefer请求头，开发页面也没有使用此发布构建，未覆盖编译后浏览器的真实接线。007增加打包拒绝条件，并使用精确发布包、本地HTTPS生产工厂和实际浏览器先验证202、Location及真实报告；不以API客户端代替前端接线证明。
- 保留006源包、失败路径及原因，不用007最终成功覆盖这次发布遗漏。首个新浏览器还遇到腾讯云“确定访问”测试域名提示；已正常点击后进入，这是验收器入口适配，不是产品新增登录。
