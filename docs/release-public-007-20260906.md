# 公网007：保留异步接线的多轮承接修正版

2026-09-06。承接006同一批用户授权修改。**本页保留007当时的记录：曾发布并承接100%流量，公网验收仅局部通过，核心“我是秦始皇”人设场景未通过，不能声明整批验收通过。后续008结果见[008发布记录](release-public-008-20260906.md)。**

匿名入口：<https://ashare-strategy-preview-305722-11-1330091763.sh.run.tcloudbase.com>。云版本为 `ashare-strategy-preview-007`，源包及公网 `/api/v1/preview-meta` 均为 `sha256:ab9ac90f989e8cfe89051d45b2dc8dfc68d3e29b7ffc60a7c7bc57437795208f`。该站仍使用临时存储，不是长期保存或重启恢复验收。

脱敏机器记录见 [public-007-acceptance.json](releases/public-007-acceptance.json)，源文件清单见 [public-007-source-manifest.json](releases/public-007-source-manifest.json)。

## 与006的差异

业务代码、对话承接、策略参数和界面不再改动。前端重新使用原有`VITE_PRIVATE_PREVIEW=true`构建，保留异步结果查询与匿名队列标识。打包器强制检查这两个关键标记，缺任一个即拒绝出包；8项定向测试通过。

本次构建命令（web目录）：

```sh
VITE_PRIVATE_PREVIEW=true VITE_USE_MOCK=false VITE_API_BASE_URL= VITE_DATA_AS_OF_DATE=2026-09-06 ./node_modules/.bin/vite build --config vite.same-origin.config.ts
```

源包`sha256:ab9ac90f989e8cfe89051d45b2dc8dfc68d3e29b7ffc60a7c7bc57437795208f`。本地专用HTTPS服务加载同一源包及生产工厂，以真实移动尺寸浏览器确认首次策略POST为202且带异步Location，再完成回测与报告分析；思考仅记录DOM采样，视口证据边界见下文。自签测试证书仅供本机验收，不修改系统信任或公网证书。

本批多轮行为、首字母换股、安全支持、数据错误分层的实现证据见[修复记录](bugfix-conversation-recovery-20260906.md)。006中间发布及验收失败单独留存，见[006记录](release-public-006-20260906.md)。

## 精确包本地验收

- HTTPS本机生产工厂与实际390×844浏览器完成策略POST 202、异步Location及匿名请求头；真实`run:91f35623287045f49b46793985bc5d86`回测成功，报告页显示计算结果。
- 报告请求同样202，实际模型思考DOM高度160px、scrollHeight471px，采样586字符。该次截图处于报告面板，不能把后台DOM尺寸说成截图已经展示了思考，也不能用这项本地采样替代公网可见性验收。
- 首次报告分析返回503，日志为模型数字复核一次后仍被拒绝的ValueError路径；报告模块与HEAD一致，不是本次业务修改引入。没有放宽校验，也没有重复回测。只对同一运行重试分析一次后200，结果哈希仍对应原运行，真实模型思考14,889字符。
- 本地结论是“报告分析一次重试后通过”，不是所有模型输出首次必成功。该输出校验稳定性留在B35收尾；已完成计算结果不受影响。
- 本次打包门禁及记录提交`4f01a9a`，已推送私有仓库。

## 公网已通过的范围

公网元信息与源包 revision 一致，`public_check.py` 已逐一下载并核对清单中的 2 个前端构建资源 SHA256。实际 390×844 浏览器完成匿名首页、策略 POST `202 + Location`、异步查询到 `201 + ready`、点击开始回测，并得到 `succeeded`。真实运行是 `run:3ac0f13cae9042cfbc4ca606834665c1`，结果哈希为 `sha256:700a4bd3044f7467477ee198c5118b30677ea872a36ac78bf830905070ad1748`。这是实际回测结果，不只是健康检查或页面可访问。

随后使用真实 API 复用该基线，连续完成以下三轮；这三轮没有新增回测：

| 输入 | 公网结果 | 不变性与执行边界 |
| --- | --- | --- |
| 股票换成dfcf。其他条件和回测设置保持不变，先不要回测。 | 3.56 秒，ready；解析为东方财富 `300059.SZ` | DSL 仅股票变化；entry、exit、backtest、execution 和 execution_settings 均与基线一致；未请求运行或强制取数 |
| 我想吃的 | 3.62 秒；“好的，那先不聊策略了。想吃点什么就好好吃，照顾好自己。” | 策略、hash、revision 和设置不变；未重复换股话术，未请求运行 |
| 明天下雨吗 | 3.56 秒；“我无法查询实时天气，建议你查看当地天气预报应用或网站获取准确信息。” | 同一草稿及规则保留；未把天气转为策略条件，未请求运行 |

换股后的草稿为 `6623374f-b712-4348-864a-adffb39d0de4`，revision 1；两次闲聊均保留该版本。

同一成功运行另发一次报告分析 POST，`202` 后于 118.08 秒返回 `200`。模型 provenance 为 `deepseek / deepseek-v4-pro`，prompt `backtest-review.prompt.v12`、schema `backtest-review.v2`，responseHash 为 `sha256:d59a227963e91182bfe40cb1043d397e61b50fe156e4ad9d736c8833fd784c11`；sourceResultHash 精确对应上述运行。返回 3 个优化候选，证据等级为 `limited`，未执行这些候选。

**公网首次浏览器报告请求的最终结果未知。** 原采集未保存其结果 Location 或报告内容，因此上述 `200` 只能记为“同一报告的另一次 POST 验证通过”，不能写成“首次明确 503 后重试成功”，也没有证据可声称缓存命中。本地那次明确 `503 → 200` 与公网这项记录互不替代。

## 公网未通过：人设承接

真实手机尺寸 UI 输入“我是秦始皇”后，异步请求最终 HTTP 为 `201`，但业务结果是 `needs_clarification`、`diagnostic_code=dialogue_model_unavailable`，strategy 与 idea_route 均为空。可见正文为“对话模型这次未能返回有效回复，请稍后重试。”，没有出现要求的风格承接或具体可编辑候选，故此核心场景失败；未发生回测 POST。

主线程回读进度时观察到模型阶段约 2.8 秒已完成。随后对 007 同版本、同一请求时间窗口的只读云日志核对，在 `2026-09-06T19:06:03Z` 找到一条 `dialogue_reply_rejected reason=unsafe_reply_or_multiple_questions`。这将问题范围收窄到回复门禁，而不是已证实的模型连接或 schema 失败。该日志不含本次 progress/draft ID，也不保存被拒的原回复；目前不能分辨是问句过多还是命中禁止话术，更不能编造具体原句。

验收脚本随后因对空 idea_route 调用 `.get()` 出现 `NoneType` 异常，这是采集器的次生错误，不改变已经保存的业务失败响应和可见错误正文。

## 思考显示与验收边界

原 `public_check.py` 在回到聊天面板后，等待报告思考的视口条件而超时。当前报告思考进度属于报告详情面板，聊天面板读取的是另一份对话进度；在错误面板等待的超时不能证明思考返回或显示已关闭。此次记录不声明该项公网视口验收通过，也不因思考字符数追加模型调用。

007 的有效结论是“发布完成；资源、基线真实回测、三轮 API 对话与另一次原报告分析局部通过；核心人设承接未通过”。008针对回复门禁的修复与独立验收另行记录，不将其后续结果写成007已经通过。未覆盖的真实设备、负载、长期存储及重启恢复仍不能由本次单浏览器/API验收推出。
