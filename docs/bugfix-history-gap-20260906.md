# B02：长历史字段缺失定位与失败提示

日期：2026-09-06。本轮改善故障定位，不表示 16 年历史数据已经补齐；没有自动缩短日期、改动规则或推算涨跌停价。B02 仍未解决，待提供方返回完整真实字段后原区间复测。

## 已核对的原始失败

- `run:dd2a74f7488a46799f3fb98a7025ec18` 与 `run:e48ded81947f4923b1707308c4e36192` 保存的策略和成交设置完全一致：贵州茅台 `600519.SH`，2010-09-05 至 2026-09-05，本金 100 万。
- 这两条是用户已改成 **10 日新高且 20 日量比超过 1.5** 的版本，卖出为回撤 8% 或持有 40 个后续交易日；不是验收基准句中的 20 日新高。原值没有静默恢复或截断。
- 滑点 0、佣金率 0.0003、最低佣金 5 元、预热 180 天等原设置均保留。结果为空，没有伪装成成功；后一条故障已定位为缺涨停价、跌停价。
- 先前本地排查记录表明，预热取数从 2010-03-09 开始；两年分段及一周合并字段查询曾失败。该项来自已有排查记录，不冒充本轮重新执行的原始响应。

## 本轮实际字段探测

通过现有 `POST /api/v1/market/query`，只查询同一只股票、2010-03-09 至 2010-03-15，一次只要一个字段。没有调用模型、创建新草稿或启动回测。

| 查询 | 实际响应 | 请求编号 | 服务记录耗时 |
| --- | --- | --- | --- |
| 贵州茅台逐交易日涨停价 | HTTP 404，`live_market_data_no_results` | `c718cd7f92a44d35801a5425850c3a72` | 5,891.913 ms |
| 贵州茅台逐交易日跌停价 | HTTP 404，`live_market_data_no_results` | `0b4e1e74351a4abab7d85339765d6e6c` | 5,417.402 ms |

这证明本次通路对上述股票、日期和字段没有匹配结果，不证明所有历史字段或所有股票都不可用，也不是模型余额不足的证据。停止继续重复同样查询。

现有茅台已验证缓存最早从 2025-02-06 开始，共 388 行至 2026-09-04；这只是本机缓存范围，不能冒充提供方的最早可用历史，更不能不考虑预热就承诺一个新的回测起点。

## 本地修复

- 缺字段异常携带当前**失败取数分段**的起止日期，包含预热时与用户回测区间分别处理。
- 失败卡会说明“查询哪一段历史时缺了什么”，明确未完成、原规则和区间保留；沿用“重新读取”“修改规则”两个入口。
- 后一个分段失败不会把前面已完成且有效的缓存删除，也不会把部分数据拼成成功结果。
- 错误只展示程序认可的字段名和日期，不展示原始服务响应、授权信息或异常正文。

## 验证边界

- 历史适配器与策略/配置保留定向回归：28 passed。
- 前端既有失败卡的重试/修改规则测试覆盖超时、缺字段两种情形：2 passed；验证重新读取仍使用原草稿且显式刷新数据。
- 独立临时页面复用真实 `FailureCard` 和应用样式，实际浏览器检查 640px/350px 卡片列：说明与按钮无重叠，两个动作点击回调可用。该页面明确标注合成故障、没有联网或执行回测；检查后已关闭并移除临时页面。这是局部排版验证，不冒充整页移动端或真实 16 年成功验收。
- 上述测试使用故障注入，不代表真实 16 年回测成功。本轮没有再发起完整 16 年回测，也未提交、推送或公开部署。
