# B28：完整 A 股名称代码本地名录

日期：2026-09-06。用户要求把所有 A 股名称和代码存下来。本次存的是提供方当前完整 A 股名录，不是只缓存已搜过的词，也不是历史退市证券全集。只做本地接入和验证，未提交、推送或公开部署。

## 已保存的数据

- 东方财富选股 Skill 通路实际返回 **5,567** 个不重复证券，深市 **2,902**、沪市 **2,320**、北交所 **345**。
- 抓取时间：`2026-09-06T10:16:49.813115+00:00`。实际行数、唯一代码数及提供方 total、totalRecordCount、stockCount 全部为 5,567。
- 应用名录：[a_share_directory.json](../ashare_lab/resources/a_share_directory.json)。包含名称、标准代码、市场、派生全拼／简拼索引，以及抓取时间和来源证明；没有行情价格、用户对话或密钥。
- 人工查看：[A股名称代码表](data/a-share-identities-20260906.csv)。UTF-8 BOM，5,567 行，名称／标准代码／交易所与应用名录逐项一致；代码保留后缀，避免表格软件吞掉前导零。
- 原始响应留在 Git 忽略的 `var/instrument-directory-source/mx-all-a-share-raw-20260906T101649Z-01d86e5f.json`，不随应用部署。完整名录只有 1 份，不上传整个运行目录。

首次带“包含 ST、停牌”等描述的查询被上游解释成筛选条件，只返回 208 条；提供方全市场计数仍为 5,567，因此该份明确拒绝作为完整名录。随后使用“全部A股，按股票代码从小到大排列，列出所有股票名称和股票代码，不限制条数。”重新获取完整结果。未把首个部分结果冒充全量。

中航成飞 `302132.SZ` 是真实代码，已按[深交所披露的公司报告](https://disc.static.szse.cn/disc/disk03/finalpage/2026-04-29/2cc6dba2-b4e5-40fa-95fb-b76a81132406.PDF)核实。只接受这一明确例外；不放宽相邻未知代码。前端市场和当前历史适配器板块一致识别，未据此宣称其完整历史已能回测。

## 接入方式

- 服务启动读取包内名录，名称片段、代码、全拼、简拼可直接匹配；无需先在线搜过这个词。
- 保留原有按查询词缓存和在线补查。新抓取的查询结果优先于较旧名录；旧名录可先展示再后台刷新该查询。名录内没有的新上市／改名股票，仍可能需要在线补查。
- 生成器检查数量、市场、身份、重复项和抓取时间，通过后才原子替换；坏新文件不覆盖当前可用名录。运行时无需拼音依赖。
- 名录已加入 Python 包资源；项目现有 Docker／源码打包路径也包含 `ashare_lab`。安装包实物已核对资源字节与源文件完全一致，包含全部 5,567 行。JSON 权限为 0644，容器普通用户可读；没有上线操作。
- CSV 只为这一份已检查的身份表添加 Git 忽略例外；行情 CSV 和 `var` 仍不纳入 Git。

## 本次验证

- 完整真实名录创建新搜索实例，禁止联网，查询“中国”“中金”“dfcf”“dongfangcaifu”“302132”“中航成飞”“920799”“平安银行”“３０００５９.SZ”均命中，网络调用 **0 次**；进程内约 2–3ms。
- 正式本地 API 五个查询均 200、来源均为完整名录；首次装载约 97ms，后续约 3–6ms。这不是含输入防抖及用户网络的端到端耗时承诺。
- 后端名录／原搜索／集成／当前历史适配器定向 93 passed；生成器 4 passed。前端搜索映射、原位编辑组件、市场上下文共 37 passed。测试夹具不是新的行情或回测证明。
- Python wheel 构建成功，锁文件仅新增可选拼音构建依赖。先前[正式页面换股及回测记录](bugfix-inline-stock-search-20260906.md)仍独立保留；本次没有重复整条模型回测，也不冒充新的页面手动验收。

## 后续更新及验收

更新全市场名录时安装项目可选依赖 `instrument-directory`，执行：

```sh
.venv/bin/python -m scripts.prepare_a_share_directory --refresh
```

复用现有东方财富凭证及客户端，一次完整查询，验证后替换；失败保留原名录。目前没有新增定时任务，也没有宣称全市场名录会自动每日更新。服务器更新、数据使用许可和正式发布仍在部署验收范围。

用户只需在现有审阅卡点击股票名，输入“中国”“中金”“dfcf”或“920799”，检查原位候选，再选择股票；股票以外的条件、日期和费用应保持。B28 待用户体验验收，B05 保持关闭。
