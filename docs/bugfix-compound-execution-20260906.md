# B06：复合条件执行核对与卖出停止原因

日期：2026-09-06。本轮沿用现有执行器，不改变买卖逻辑、成交时点、卖出尝试预算或历史报告。B19／B20 仍是未获确认的独立设计，不在本轮实施。

## 核对结果

以下为真实应用模块的确定性测试，输入是隔离的合成日线／指标，不代替真实数据逐点验收。

| 条件 | 当前实际语义 | 定向验证 |
| --- | --- | --- |
| RSI14 从下向上穿过30，且当日成交额高于5亿元 | 前值小于等于30、当值大于30；同一时点的成交额必须严格大于5亿 | 30边界、成交额等于5亿不通过；RSI与成交额不得错日拼接 |
| 全部满足／任一满足 | 保留不同的逻辑关系及未知值，不把缺值当成真 | all／any 三值真值表均通过 |
| RSI高于55／持仓亏损5%／持有10个后续交易日，任一先发生退出 | 首次已确认的退出决策继续执行；不被后来条件替换 | 三个出口分别最先触发均通过，等待卖出时不重选原因 |
| RSI与5%止损 | 日线收盘确认，之后可用开盘尝试；不是盘中逐笔止损 | 收盘与下一开盘有明确先后，盘中低点不额外触发 |
| 持有10个后续交易日 | 首次实际买入成交后的第10个日历服务交易时段，目标开盘尝试；停牌日仍占该市场时段 | 目标日停牌、随后跌停时，在已配置预算内继续尝试 |
| 复合买入条件有效期 | 当前合同规定 all／any／not 组合为一次委托机会，不偷偷扩成三天 | 原决策不会无限延续；不是此轮待修bug |
| 卖出重试 | 受 retry_unfilled_exits 和 max_exit_attempts 限制 | 20次预算不会产生第21次；关闭重试只尝试一次 |

上述后两项对应现有 SPEC 的信号有效期与尝试预算合同。本轮没有为了让测试成功而放开限制，也没有改变后续独立新信号可再形成新决策的行为。

## 本轮实际修复

原有活动记录已经包含每次未成交原因，但卖出预算耗尽或重试被关闭后，只清除了等待状态，缺少明确的“本次决策已停止尝试”终态。

- 保留真实未成交／部分成交活动，另加关联同一决策、最后一张实际订单的终态。记录实际尝试次数，不新增订单、成交或现金变化。
- 部分成交后停止，仅说明未卖出的部分仍保留；已全平不产生错误终态。停止后出现独立新信号，仍按原规则执行。
- 当前链路详情使用“卖出尝试已结束”，正文分别为“已达到卖出尝试上限，未卖出部分保留；可调整成交设置后重新回测”和“成交设置关闭了卖出重试，未卖出部分保留；可调整设置后重新回测”。原因只说明一次；工程代码保留在原有折叠技术详情中。
- 通过已有 unfilled／expired 合同传输。新增的是停止记录，不代表又提交了一张失败订单。旧结果不重写，新结果指纹包含新增记录。

涉及执行器 `skill_backtest.py`、现有结果序列化器 `skill_backtest_service.py`、现有 `web/src/view-model.ts`；没有新增页面或改变布局。文案按 impeccable 的清晰异常说明规范整理，正常模型正文不受影响。

## 原美的真实结果的只读追溯

另只读核验了此前六年运行 `run:f9c3578517884bc49eded44db0915d2e`，不是本轮重新取数或重新回测。保存结果 succeeded，8个完整回合、48条活动，结果指纹与重算一致：

`sha256:28a3faa54e2df968fc84136a1e16e95a430c3c9da0bdc1262e0e4959f301b303`

8笔买入均为收盘信号后下一交易日开盘代理；5笔RSI／止损退出同样遵循收盘确认后开盘代理。其余3笔持有期退出分别为：

| 实际买入成交日 | 第10个后续交易时段退出日 |
| --- | --- |
| 2022-03-31 | 2022-04-18 |
| 2024-07-29 | 2024-08-12 |
| 2026-03-25 | 2026-04-09 |

这些活动可核对保存的触发时点、原因及持有期顺序，**不能独立证明每一天RSI和成交额数值均正确**：旧结果未保存完整指标值与不可变数据版本，当前磁盘也没有该股票对应旧值。重新获取今天的数据不能反向补证旧运行。B06 保持局部通过，真实逐点数值核验尚未收尾；不把执行器合成测试当成全部真实数据验收。

## 验证和用户验收

- 生产工作区：新增直接文件15项，加既有收盘成交／停牌涨跌停重试两项，共 **17 passed**。每个新增执行场景经过现有结果包schema及hash核验；Ruff通过，相关三文件Pyright 0 errors。
- 前端 `view-model.test.ts` 全文件 **22 passed**，TypeScript通过；新两种终态各验证一张实际订单合并、部分成交保留、原因翻译与技术代码隔离。impeccable定向检测未发现机械性问题。
- 在独立本地标签页复用实际 `ChainScreen`、实际样式和 `buildChain`，渲染明确标注为“合成事件”的两种终态。桌面截图与DOM核对正文、次数及技术详情展开通过；用户原标签页未操作，测试标签已关闭。不是完整真实回测UI验收，也未声称手机端已复验。
- 用户正常验收仍按总清单A策略操作，不要求用户手工构造20天停牌。异常终态由上述工程测试负责；用户可检查遇到真实未成交时说明是否清楚。

未提交Git、推送或公开部署；B05保持已验收关闭。
