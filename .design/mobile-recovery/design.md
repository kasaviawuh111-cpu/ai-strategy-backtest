# 手机回测反馈：A已实施；报告导航v2待确认

## 最新反馈：报告页的左箭头挤压标题

用户最新截图属于 `detail-top`，不是前面已改的审阅栏。真实代码里左箭头34px加10px间隔，且 `detail-meta / detail-tabs / detail-panel` 都额外左缩进44px；右侧编辑按钮又占一列，手机整页有效宽度减少。

`v2.html` 提案：手机顶部单独一行“返回对话 / 编辑策略”，标题在下方通栏且内容完整；股票、页签、报告统一左对齐，手机44px左缩进归零。保留既有配色、衬线字和报告内容，标题行高由1.22增至1.4；桌面不变。

仅生成独立预览，尚未修改正式报告页。返回与编辑预览只演示保留当前对话／打开原审阅的落点，不请求数据。下方报告仅复用已见页面的结论快照；图表和明细未重画，源代码层对齐核对完成，但本地文件浏览器自动渲染受限，未声称v2已经视觉验收。

v2内联JavaScript与元数据语法检查通过。静态样式检测提示既有Inter字体及模板字级接近；本提案按用户要求保留产品字体与组件尺寸，不借此整体换风格。预览已提交到应用文件面板，实际预览由用户确认。

## 已批准的手机方案A

2026-09-06后续决定：用户选择A，限定手机版。B31受理后回到对话展示进度、提交失败可见并保留重试参数，以及B32独立顶部“返回对话”已本地实施。桌面原入口与分栏不变；尚未部署。实现与真实链路证据见 `docs/bugfix-mobile-progress-20260906.md`。

B30首页chip兼容恢复仍未实施。下方保留早期方案对比／预览验证记录，不把它们等同于本轮实际产品验证。

## 对比范围

- 用户与目标：手机微信网页访问者，生成策略后能够看见回测状态，并在返回／重新进入后继续使用快捷例句。
- 基准：线上003、用户10秒手机录屏、App／SummaryCards／ThinkingStream 及产品tokens。
- A（推荐）：接受回测后收起审阅，回到对话显示现有进度、结果或失败卡。股票和条件仍在上方可打开审阅。
- B：保持审阅，在操作区域直接露出同一进度／失败反馈；不中断看规则，但占用更多手机空间。
- 两方案共同的返回适配：网页审阅顶部使用“返回对话”，箭头与文字有完整点击区域，和策略标题分开，不再同屏保留重复的 ×。录屏底部的左箭头属于微信浏览器，网页不能改变它。
- 2026-09-06 用户补充范围：上述顶部导航及 A“回到对话看进度”只针对手机版，沿用现有手机断点；桌面仍保留左右分栏和原关闭入口。此为范围确认，正式页面仍未实施，统一放入下一批发布清单。
- 首页示例：显示新进入、生成中、结果连接中断、恢复查询。预览里的计时与状态是演示，不是模型实时信息。
- 不改：买卖规则、资金／费用／区间、股名解析、历史报告、B19条件分组设计。

## 已确认与未确认

已确认手机审阅打开时隐藏对话，而运行和失败卡在对话内；按钮恢复不代表用户看到了终态。

独立兼容实验已证明缺失 `AbortSignal.any` 时，POST受理后第一次查询结果前会报错。未取得用户手机实际API支持信息，所以它不是被确认的唯一根因。微信自身确认访问页也不能由此产品预览控制。

## 预览验收

在 A/B 下点“开始回测”，切换正常处理、暂时断线和最终失败，检查能否立刻看到状态及安全恢复动作；点顶部“返回对话”检查返回落点；再用首页例句重新生成。所有按钮仅操作预览内存，不调用模型、不查行情、不执行回测。

独立HTML按390px产品画面与桌面对比版心制作；内联JS语法检查通过，无外部资源或网络请求。浏览器工具的本地文件URL策略阻止了自动打开，未绕过限制，因此本预览的实际渲染和点击验收仍未完成；不是已完成视觉QA的记录。源码检查保留现有字体、编号虚线、黑色主按钮和状态样式，自动样式提示不触发本轮无关重设计。

上述是早期预览验证边界。后续A本地真实回测已完成；实际iPhone微信、线上兼容和发布仍待验收，不把桌面窄视口当成真机通过。
