# 031 发布后补充修复记录

日期：2026-09-15。基线为 `c90966c`（031 发布及验收记录）。本记录属于后续工作区修复，不属于已发布 031。

## 修复范围

- 表格压缩采用无损 `columnar.v1`。生成与独立审核现在共用同一解码说明，每张表按自身 `columns` 解释行值，不套用外层列顺序，不跨表按行号合并，不删减行列。
- 股票与策略配对的说明被拒后，现有一次修复调用会收到被拒原文及针对性反馈。指标筛选仍核对指标，业务主题仍核对业务，不将低 PE 等请求改成行业筛选。
- 明确配对、选中方案、历史数据可用和回测完成是不同状态，禁止在尚未核对数据时承诺“确认后就能看回测”。成交额、换手率也不能直接证明波动空间。
- 保留独立事实审核、执行边界校验及重试上限。有效的负面审核结论不靠重复审核强行放行。

## 最小验证

| 检查 | 结果 | 证据与边界 |
| --- | --- | --- |
| 相关自动测试 | 46/46 通过 | `test_vibe_strategy_advice.py` 与 `test_reply_review_recovery.py`；包含不同列顺序、null、补充表、修复上下文及有效拒绝不能绕过 |
| 差异检查 | 通过 | `git diff --check`；本次新增审核测试文件 Ruff 通过。其他被修改文件有既有 lint 问题，未宣称全库 lint 通过 |
| 低 PE + MACD，修改前 | 未通过 | 同一句真实请求 89.97 秒，最终 `stock_pairing_pending`；有说明审核失败及过早回测承诺，不认定全部拒绝都是误判 |
| 低 PE + MACD，修改后 | 草稿路径通过 | 132.21 秒，HTTP 201 返回 3 个有实际证券数据的候选，选择首项 HTTP 200 / `ready`；保留 PE 建议阈值与 MACD 金叉买、死叉卖 |
| 荔枝交易灵感，修改后 | 外部阻塞，未完成 | 首轮执行边界不一致被拒，现有 Pro 修复后已进入展示审核；审核调用返回 DeepSeek HTTP 402 / `insufficient_balance`，对外 HTTP 503，没有伪造成功 |

真实模型用例是原 20 条中的第 17、18 条。当前本轮 2 条中，1 条草稿路径通过、1 条因模型余额不足未完成，不能据此宣称整批 20 条通过或回测完成。新测试使用隔离临时数据库和真实模型/公开证券数据，未使用用户持仓，未执行历史回测。

低 PE 首项绑定云铝股份 `000807.SZ`，返回当日 PE TTM 为 8.55 倍，所选方案为模型建议的 PE 低于 15 且 MACD 金叉买入、MACD 死叉卖出。其策略哈希为 `sha256:fc6524421383a4bb2330aeb1413000dc876141415ee7ba9b6092e05edb78462a`。这只是生成与选择一致性的测试材料，不是投资建议；历史 PE 数据及真实回测终态未在本轮验证。

## 服务与发布状态

- 线上仍为已验收的 031，本次没有部署或切流。
- 当前本地 8011 / 5184 服务未重启，仍运行前一版本。本次后端源码修复只在隔离测试进程验证。
- 用户最终确认的使用说明及删除“例如”句子已在此前 `63092c4` 完成，属于 031 发布内容，不受本次改动影响。
- 待模型余额恢复后完成未完用例，再更新本地供用户验收。未获本轮本地验收不得发布本次修复。

## 证据

- 修改前：`remaining-pairing-before/low_pe_macd_pairing.json`，请求 `8a706adb34ce4a9c8d5fa68e8ba61892`。
- 修改后：`remaining-pairing-after/low_pe_macd_pairing.json`，请求 `a9d53c3f77ab4469a62e7430203ffb1f`；`model-7-dialogue_reply_semantic_review.json` 为独立审核通过结果。
- 灵感补测：`remaining-lychee-after/ambiguous20_lychee.json`，请求 `1167e77ec5684783b733c261604f16a9`。
- 复现命令：`.venv/bin/python outputs/single-strategy-order-regression-20260915/probe.py <case> <output-directory>`；case 分别为 `low_pe_macd_pairing`、`ambiguous20_lychee`。凭据只从已有钥匙串读入内存，不写入测试结果。
